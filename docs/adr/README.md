# Architecture Decision Records

ADRs record material technical decisions and any intentional departure from
`docs/master-specification.md`. The master specification locks capabilities and
invariants; ADRs lock the current implementation choices.

Each ADR states status, context, decision, alternatives considered,
consequences and reversibility.

| # | Decision | Status |
| --- | --- | --- |
| [0001](0001-application-architecture-and-versions.md) | Application architecture and supported-version policy | Accepted |
| [0002](0002-database-and-identifier-strategy.md) | Database and identifier strategy | Accepted |
| [0003](0003-document-lifecycle.md) | Document lifecycle: immutable evidence and mutable working documents | Accepted |
| [0004](0004-authentication-direction.md) | Authentication: synthetic local sign-in now, Entra ID for real data | Accepted |
| [0005](0005-authorization-and-visibility-inheritance.md) | Authorization and visibility inheritance | Accepted |
| [0006](0006-search-architecture.md) | Search architecture: PostgreSQL first, through a rebuildable projection | Accepted, implemented in Stage 2A |
| [0007](0007-reporting-continuity-and-export-contract.md) | Reporting continuity and the export contract boundary | Accepted, exporter in Stage 4 |
| [0008](0008-production-deployment-candidate.md) | Production deployment candidate | Proposed, deliberately reversible |
| [0009](0009-design-token-foundation.md) | Design-token foundation | Accepted |
| [0010](0010-stage-1-interaction-and-browser-testing.md) | Stage-1 interaction model and browser testing | Accepted |
| [0011](0011-next-action-and-submission-modelling.md) | NextAction and Submission modelling | Accepted |
| [0012](0012-legacy-register-import.md) | Legacy register import architecture | Accepted |
| [0013](0013-search-projection-and-child-content.md) | The search projection, and why child content waits | Accepted |
| [0014](0014-content-extraction-and-derivatives.md) | Content extraction and rebuildable derivatives | Accepted |
| [0015](0015-historical-corpus-integration.md) | Historical corpus integration | Accepted |
| [0016](0016-authentication-modes-and-the-shared-gate.md) | Authentication modes and the shared gate | Accepted |
| [0017](0017-statistics-and-the-metric-catalogue.md) | Statistics, the metric catalogue and operational snapshots | Accepted |
| [0018](0018-structured-matter-facts.md) | Structured Matter facts, and the generated department views | Accepted |
| [0019](0019-opinion-archive-reconciliation.md) | Reconstructing historical submissions from the opinions archive | Accepted |
| [0020](0020-historical-cutover-current-state.md) | The historical cutover, and what a closed archive row may claim | Accepted |
| [0021](0021-final-register-cutover.md) | The final register cutover, and the two columns that mean different things | Accepted, amended 2026-09-26 |
| [0022](0022-deployment-backup-and-recovery.md) | Deployment, backup and recovery on the Unraid host | Accepted |
| [0023](0023-searchable-opinion-archive.md) | Making the whole opinions archive searchable evidence | Accepted |
| [0024](0024-test-data-classification.md) | Test data is a stored class on the Matter, and purging it is a later decision | Accepted |
| [0025](0025-multiple-matter-senders.md) | A Matter has zero, one or several senders; the addressee stays singular | Accepted |
| [0026](0026-source-data-enrichment.md) | Source facts are never rewritten; interpretation is added on top of them | Accepted |
| [0027](0027-matter-engagement.md) | `Kaasamine` is a pointer to outreach, not an engagement system | Accepted |
| [0028](0028-development-archive-workspace-access.md) | The archive is readable behind the shared gate; the register is not | Accepted |
| [0029](0029-reference-data-foundation.md) | Reference data is governed, additive, and never invented from source strings | Accepted |
| [0030](0030-teema-workspace-redesign.md) | The Teema workspace: one page, two tabs, and one save per professional update | Accepted |
| [0031](0031-teema-human-qa-corrections.md) | What hands-on use changed about the Teema workspace | Accepted, amended 2026-09-26 |
| [0032](0032-uus-teema-redesign.md) | The approved Uus teema redesign: one screen, one chip control | Accepted, amended 2026-09-16 |
| [0033](0033-overview-drilldown-parity.md) | Ülevaade drill-down parity: every number opens a list of its own kind | Accepted |
| [0034](0034-persona-candidates-and-the-top-bar-switcher.md) | Persona candidates are a role-based population, and switching happens from the bar | Accepted |
| [0035](0035-the-bounded-workspace.md) | The shell keeps the viewport; the workspace stops at 1600px and centres | Accepted |
| [0036](0036-assignable-department-workers.md) | Who current business work may be assigned to, and who a filter may name | Accepted, amended 2026-09-11 |
| [0037](0037-the-business-write-http-boundary.md) | Where business-write authorization is enforced, and why the refusal is 404 | Accepted |
| [0038](0038-child-visibility-in-projections.md) | Child visibility in projections, and the index-version gate | Accepted |
| [0039](0039-retiring-minu-tiim.md) | Retiring `Minu tiim`, and where its three period counts went | Accepted |
| [0040](0040-concurrent-final-evidence-integrity.md) | Concurrent final-evidence integrity: one lock order, one strength | Accepted; §3 amended by 0109 |
| [0041](0041-search-index-freshness.md) | Search index freshness: durable debt instead of a rebuild somebody remembers | Accepted; a manual rebuild discharges its debt since 0114 |
| [0042](0042-department-wide-lawyer-access.md) | Department-wide access for the legal team | Accepted |

| [0043](0043-the-ux-pass-and-the-managers-page.md) | The 2026-08-27 UX pass: an additive layer, views that live in the address, and Osakond narrowed to three questions | Accepted |
| [0044](0044-one-arvamused-destination-and-live-header-search.md) | One `Arvamused` destination, and live suggestions under the header search | Accepted |
| [0045](0045-the-repeatable-current-register-refresh.md) | The repeatable current-register refresh: authorship decides, and the sheet's own year settles a date | Accepted |
| [0046](0046-two-deadline-groups-a-week-and-the-rest-of-the-month.md) | Tähtajad in two groups: the calendar week whole, the rest of the month behind «Näita veel», and one line past it | Accepted |
| [0047](0047-arvamused-as-a-section-of-teemad.md) | Arvamused as a section of Teemad, with two searches that never meet | Accepted |
| [0048](0048-the-v2-design-implementation.md) | Implementing the v2 design over the application that exists | Accepted |
| [0049](0049-one-department-page.md) | One department page: `/ulevaade/` and `/osakonna-too/` become `/osakond/` | Accepted |
| [0050](0050-an-open-jargmiseks-outranks-arvamuse-tahtaeg.md) | An open `Järgmiseks` outranks `Arvamuse tähtaeg` in the work model | Accepted |
| [0051](0051-uus-asi-new-assignment-notices.md) | «Uus asi»: a personal receipt for a Matter somebody just put on your desk | Accepted |
| [0052](0052-the-simplified-teema-next-action-workflow.md) | The simplified Teema next-action workflow | Accepted; §5's «a step needs a date» row superseded by 0106, the rest stands |
| [0053](0053-the-1-september-snapshot-and-two-spellings-the-parser-could-not-see.md) | The 1 September snapshot, and two spellings the parser could not see | Accepted |
| [0054](0054-the-action-kind-is-not-a-user-facing-concept.md) | The action kind is not a user-facing concept | Accepted |
| [0055](0055-what-counts-as-evidence-for-an-archive-link.md) | What counts as evidence that a letter belongs to a Matter | Accepted |
| [0056](0056-the-archive-is-department-work-product.md) | The opinion archive is department work product, not a migration tool | Accepted |
| [0057](0057-a-baseline-holds-the-product-not-the-clock-or-the-seed.md) | A baseline holds the product, not the clock and not the seed | Accepted |
| [0058](0058-barlow-is-the-typeface.md) | Barlow is the typeface, not a fallback for one | Accepted |
| [0059](0059-what-a-valja-mark-says-about-an-arvamuse-tahtaeg.md) | What a `VÄLJA` mark says about an `Arvamuse tähtaeg` | Accepted |
| [0060](0060-deterministic-document-assisted-intake.md) | Deterministic document-assisted intake: suggestions, not facts | Accepted |
| [0061](0061-matter-opinions-live-under-documents.md) | A Matter's opinions are documents, and the per-Matter Arvamused page is retired | Accepted, amended 2026-09-26 |
| [0062](0062-related-materials-and-human-confirmed-links.md) | Seotud materjalid: derived suggestions, human-confirmed links | Accepted |
| [0063](0063-one-organisation-catalogue-behind-saatja-and-adressaat.md) | One Organisation catalogue behind Saatja and Adressaat, and a sender may be named where it is used | Accepted; «two controls, on purpose» superseded by 0073 |
| [0064](0064-assisted-intake-happens-while-the-teema-is-being-created.md) | Assisted intake happens while the Teema is being created | Accepted; session is the form token since 0112 |
| [0065](0065-adding-a-structured-fact-happens-on-the-matter.md) | Adding a structured fact happens on the Matter, not on a page of its own | Accepted |
| [0066](0066-the-malware-scan-gate-is-a-real-scanner.md) | The malware scan gate is a real scanner | Superseded by 0072 |
| [0067](0067-uus-teema-asks-two-questions-about-one-catalogue.md) | `Uus teema` asks two questions about one catalogue, and creates real work | Accepted; the «Vali nimekirjast» disclosure superseded by 0073 |
| [0068](0068-the-snapshot-records-which-step-it-photographed.md) | The operational snapshot records which step it photographed | Accepted |
| [0069](0069-the-saatja-is-the-adressaat-by-default.md) | On `Uus teema` the Saatja is the Adressaat, and Adressaat folds away | Accepted |
| [0070](0070-oigusakt-is-a-canonical-matter-field.md) | `Õigusakt` is a canonical multi-select Matter field, distinct from Menetlusliik | Accepted |
| [0071](0071-teemad-is-the-one-discovery-surface.md) | Teemad is the discovery surface, and «Tähtajad» is not a destination | Accepted |
| [0072](0072-document-reading-is-an-ephemeral-intake-capability.md) | Document reading is an ephemeral assisted-intake capability | Accepted; its metadata-search consequence amended by 0113 |
| [0073](0073-one-control-finds-and-names-an-institution.md) | On `Uus teema`, one control finds an institution and names a new one | Accepted |
| [0074](0074-teema-approved-target.md) | The Teema detail page is the approved target | Accepted, amended 2026-09-26 |
| [0075](0075-teema-current-action-and-add-to-matter-workspace.md) | The Teema current action and add-to-matter workspace | Accepted; §3's premise amended by 0107, its decision stands; §4 amended by 0110; amended 2026-09-26 |
| [0076](0076-closed-matter-dokumendid-is-read-only.md) | A closed Matter's Dokumendid surface is read-only for business work | Accepted |
| [0077](0077-a-personal-note-autosave-cannot-overwrite-a-newer-one.md) | A personal note's autosave cannot overwrite a version it never saw | Accepted |
| [0078](0078-lisa-teemale-is-a-stable-choice-bar-and-kaasamine-asks-for-its-dates.md) | `LISA TEEMALE` is a stable choice bar, and `+ Kaasamine` asks for its dates | Accepted |
| [0079](0079-dates-are-recorded-at-the-precision-they-are-known-to.md) | A planned date is recorded at the precision it is actually known to | Accepted |
| [0080](0080-oigusakt-is-read-from-one-documents-head.md) | `Õigusakt` is read from one document's head, not pooled over the envelope | Accepted |
| [0081](0081-kodulehe-ulevaade-is-a-dedicated-matter-activity.md) | `Kodulehe ülevaade` is a dedicated Matter activity, and `koda.ee` is its boundary | Accepted; the name and §3's `koda.ee` boundary superseded by 0085 |
| [0082](0082-kaasamise-kuupaev-is-recorded-at-the-precision-it-is-known-to.md) | `Kaasamise kuupäev` is recorded at the precision it is known to | Accepted |
| [0083](0083-a-feedback-deadline-is-a-dated-point-and-an-overview-can-arrive-published.md) | A feedback deadline is a dated point, and an overview can arrive already published | Accepted, amended 2026-09-16 |
| [0084](0084-a-valine-seisukoht-is-a-dedicated-matter-activity.md) | `Väline seisukoht` is a dedicated Matter activity, and its source is the boundary | Accepted, amended 2026-09-16 |
| [0085](0085-an-overview-or-news-item-is-one-neutral-publication-activity.md) | An `Ülevaade / uudis` is one neutral publication activity, and any public web address will do | Accepted; §2's labelled link superseded by 0105 §3, its reason kept |
| [0086](0086-a-kaasamine-waits-for-answers-and-somebody-finishes-it.md) | A `Kaasamine` waits for answers, and somebody finishes it | Accepted |
| [0087](0087-the-similar-matter-finder-extends-one-engine.md) | The similar-Matter finder extends one engine, and reaches `Uus teema` | Accepted; §4's GET amended to POST by 0108, the rest stands |
| [0088](0088-uus-teema-is-manual-first.md) | `Uus teema` is manual-first: the reading is withdrawn, Saatja starts empty, Valdkond folds away | Accepted |
| [0089](0089-menetluse-link-is-a-reference-and-a-publication-date-is-optional.md) | `Menetluse link` is a reference, and a publication date is optional | Accepted |
| [0090](0090-one-question-per-classification-on-uus-teema.md) | One question per classification: the reviewed Hetkeseis and Õigusakt vocabularies, and the three questions `Uus teema` asks | Accepted |
| [0091](0091-one-lawyer-workflow-from-teema-to-the-next-arvamus.md) | One lawyer workflow, from `Teema` to the next `Koja arvamus` | Accepted, amended 2026-09-26 (§2: the engagement date-order rule is the services', read against the whole period); §1.2's reasoning cites a constraint 0106 dropped — the rule it protects is unchanged and enforced by the service alone |
| [0092](0092-substantive-matter-history-and-evidence-safe-process-timeline.md) | Substantive Matter history, and an evidence-safe process timeline | Accepted; the phase grouping of `Teema käik` is retired by 0105 §1, §2's placement of `Menetluse kulg` stands |
| [0093](0093-an-opinion-carries-its-own-marksonad-and-its-own-ulevaade-links.md) | A `Koja arvamus` carries its own `Märksõnad` and its own `Ülevaade / uudis` links | Accepted |
| [0094](0094-uus-teema-answers-with-menus-and-one-date.md) | `Uus teema` answers with menus, and asks for one date | Accepted |
| [0095](0095-simpler-opinion-and-feedback-capture.md) | Simpler opinion and feedback capture on the Teema page | Accepted |
| [0096](0096-uus-teema-is-the-master-and-a-teema-can-be-deleted.md) | `Uus teema` is the master, and a Teema can be deleted | Accepted; §1's three edit-only fields and the launcher's twelve chips are superseded by 0097; the deletion protocol stands unchanged; §4.6 amended by 0111 |
| [0097](0097-teema-andmed-lisa-teemale-teema-toimingud.md) | `Teema andmed`, `Lisa teemale`, `Teema toimingud` | Accepted |
| [0098](0098-a-file-reads-in-phases-and-the-road-ahead-is-conditional.md) | A file reads in phases, and the road ahead is conditional | Accepted; succeeds 0092 §13–§17 and reverses its «replacing the dated strip» rejection narrowly; the history's own phase headings are retired by 0105 §1, the rail's are not |
| [0099](0099-one-rail-and-the-lawyer-tailors-it.md) | One rail, and the lawyer tailors it | Accepted; amends 0098, reverses 0097 §9 and 0092 §13's stage-on-the-node amendment; amended 2026-09-21 (deadline placement, `Lisa fail`, unplaced copy) |
| [0100](0100-the-rail-is-the-road-ahead-and-teema-kaik-is-the-record.md) | `Menetluse kulg` is where the file is going; `Teema käik` is what happened | Accepted; succeeds 0099 and amends 0098 §8 and 0074 §12.1 |
| [0101](0101-received-feedback-may-name-nobody.md) | Received feedback may name nobody | Accepted; relaxes 0091 §3.3 for `RECEIVED` rows only |
| [0102](0102-a-mistaken-record-comes-off-the-active-file.md) | A mistaken record comes off the active file | Accepted; reverses 0084 §8's «no delete» rule for user-created `Teema käik` blocks |
| [0103](0103-a-recorded-send-is-correctable-and-a-position-takes-a-later-paper.md) | A recorded send is correctable, and a position takes a paper that arrives later | Accepted; amends 0061 and 0093 §3, extends 0084 §8 |
| [0104](0104-every-whole-record-editor-carries-its-version.md) | Every whole-record editor carries the version it was filled from | Accepted; generalises the existing per-record pattern, no migrations; §1 extended by 0109 |
| [0105](0105-teema-kaik-is-one-list-and-a-marge-takes-whatever-there-is.md) | `Teema käik` is one list, and a `Märge` takes whatever there is | Accepted, amended 2026-09-26 (§4: a `Märge` is judged by what it writes, on the locked Matter, and a correction may not empty it); retires 0092's phase grouping, reverses 0085 §2's link label, one migration; §4's required next-step date superseded by 0106 |
| [0106](0106-a-next-action-may-have-no-date-yet.md) | A next action may have no date yet | Accepted; supersedes 0052 §5's date requirement and drops its constraint, one migration |
| [0107](0107-a-column-swap-keeps-what-was-typed-and-a-failure-is-told.md) | A column swap keeps what was typed, and a failed request is told | Accepted; amends 0075 §3's premise, not its decision; no migration |
| [0108](0108-similar-matters-asks-by-post-with-only-its-deciding-fields.md) | `Sarnased teemad` asks by POST, with only the fields that decide it | Accepted; amends 0087 §4 and reverses its rejected POST alternative; no migration |
| [0109](0109-matter-writes-lock-the-matter-before-the-search-index.md) | Matter writes lock the Matter before the search index, and inline whole-value editors carry the version of their own value | Accepted; amends 0040 §3 and extends 0104 §1 |
| [0110](0110-workflow-transitions-decide-on-the-locked-row.md) | Workflow transitions and owner assignment decide on the locked row | Accepted; amends 0075 §4 |
| [0111](0111-a-cross-matter-pointer-locks-both-matters.md) | A write that points one Matter at another locks both | Accepted; amends 0096 §4.6 |
| [0112](0112-an-uus-teema-form-is-saved-at-most-once.md) | An `Uus teema` form is saved at most once | Accepted; extends 0064's intake session; one additive migration |
| [0113](0113-every-document-is-findable-by-name.md) | Every document is findable by its name, and the projection holds text | Accepted; amends 0072; `INDEX_VERSION` → `DOKUMENT.1`, rebuild required |
| [0114](0114-search-checks-check-what-they-claim.md) | Search checks check what they claim, and the manual repair repays | Accepted; amends 0041 |
| [0115](0115-ci-proves-what-it-says.md) | CI proves what it says it proves | Accepted; no migration |
| [0116](0116-search-answers-from-its-indexes-and-every-result-is-reachable.md) | Search answers from its indexes, and every result is reachable | Accepted; resolves the open search-scale decision; amends 0014 (trigram indexes); one index-only migration |
| [0117](0117-search-finds-estonian-word-forms-and-says-what-matched.md) | Search finds Estonian word forms, and says what matched | Accepted; `INDEX_VERSION` → `SONAVORM.1`, rebuild required; one migration |
| [0118](0118-a-full-rebuild-builds-a-new-generation-beside-the-one-in-use.md) | A full rebuild builds a new generation beside the one in use | Accepted; amends 0013 and 0041; one migration; no `INDEX_VERSION` change |

Naming: `NNNN-short-decision-title.md`.

## Stage coverage

- 0001–0009 — Stage 0 foundation
- 0010–0011 — Stage 1 vertical slice
- 0012–0013 — Stage 2A import and search foundation
- 0014 — Stage 2B evidence and content intelligence
- 0015–0016 — Stage 2D historical corpus and authentication modes
- 0017 — Stage 2E statistics and reporting
- 0018 — Stage 2G structured Matter facts
- 0019 — Stage 2H opinion archive and historical submissions
- 0020 — Stage 2I historical cutover state
- 0021 — the final register cutover — **amended 2026-09-26**: «Arvamus koostamisel» is one per-Matter definition (`register_filters.opinion_state_q`) behind Osakond's ARVAMUS KOOSTAMISEL column, its drill-through and `?arvamus=`: a DRAFT Submission the reader may see, or a CURRENT register row with a blank `VÄLJA` that no readable SENT Submission has overtaken; `saadetud` is a readable SENT Submission or a `VÄLJA` mark; native Matters count, a native send retires a stale blank cell, `VÄLJA` itself means what it meant, and WITHDRAWN/SUPERSEDED populations are left to ENG-062
- 0022 — deployment, backup and recovery on the host the system actually runs on
- 0023 — Stage 2H.2, the searchable opinion archive
- 0024 — real/test data classification and the purge plan that precedes a purge
- 0025 — multiple Matter senders, and the addressee that stays singular
- 0026 — Wave 2 source-data enrichment: JÄRGMISEKS, OneNote filing structure and historical activity
- 0027 — Wave 2 Kaasamine: how members and stakeholders were asked
- 0028 — the development archive workspace: who may read the corpus behind the shared gate, and why that is not the register
- 0029 — the reviewed reference-data baseline: nine policy areas, the core public institutions, and why no backfill yet
- 0030 — the approved Teema workspace redesign: two tabs, one composer save, grouped-not-merged history, and the twenty-three working Valdkonnad
- 0031 — what a working session on real data changed: the position back in the rail, one dated work list, a full edit page beside the inline controls — **amended 2026-09-26**: the first owner an unowned Matter gets also takes the open step nobody holds (`responsible` NULL), so an Uus teema «Koostan arvamuse» step triaged to a lawyer lands on their Minu asjad; a step a named colleague holds is still never taken, and the MATTER_ASSIGNED `next_action_moved` names exactly the step that moved
- 0032 — the approved Uus teema redesign: one screen, one chip control, two Valdkonnad withdrawn, and Hetkeseis explaining itself on the row
- 0033 — Ülevaade drill-down parity: a number opens a list of its own kind, `?too=` makes the dated-work populations addressable, and "Ootab pahavarakontrolli" stops telling readers their archive might be infected
- 0034 — who may be offered as a persona: one role-based population read by both the page and the endpoint, technical accounts excluded, and a top-bar switcher that keeps the page somebody was reading
- 0035 — the bounded workspace: the shell keeps the viewport, the working surface stops at 1600px and centres, and one token says so for every page
- 0036 — who current business work may be assigned to: one role-based rule behind persona and assignment, a separate native entry point so a new step never lands in a departed colleague's queue, and filters that describe stored work instead of narrowing to who may be given it — **amended 2026-09-11**: the exemption this ADR granted Osakond's Meeskond table is withdrawn, a named row there now means a current `department_workers()` member rather than anybody who owns work, `CASEWORK_ROLES` is deleted, and every visible count owned outside the department is aggregated into one anonymous `Väljaspool osakonda` row that keeps `Kokku` reconciling without naming anybody
- 0037 — where business-write authorization is enforced: one HTTP decorator over the existing `may_write_business_content` rule, applied before the view body, answering 404 so a reader learns nothing about what exists for somebody else — and a completeness test that fails if a future mutating route arrives unclassified
- 0038 — child visibility in projections: a `Kaasamine` gets its own search row rather than being folded into the Matter's, one `ChangeEvent` scoping helper for the timeline, filter labels resolved inside the reader's own data, and an index-version gate that makes pre-fix search rows ineligible on deploy rather than after a rebuild
- 0039 — retiring `Minu tiim`: one operational overview population, its three period counts moved into `Aruandlus` as rows of that block, `?vaade=tiim` normalizing to `Kogu osakond`, and no population widened anywhere in the move
- 0040 — concurrent final-evidence integrity: both writers serialise on the Matter row under one global `Matter → Submission → Document` order, the waiter re-reads what it decides on, a `matter_id` trigger refuses reparenting relied-upon evidence, and no automatic repair — DATA-001 keeps the detector
- 0041 — search index freshness: a high-fanout rename records a durable obligation in its own transaction instead of nothing at all, a Compose worker discharges it with the atomic rebuild, `Kaasamine` becomes a bounded synchronous refresh so a recorded consultation is a findable one, and the diagnostic reports the debt without ever draining it
- 0042 — department-wide access for the legal team: the confidentiality boundary is the application rather than the Matter, both lawyer roles read the whole department, ownership and collaborators go back to meaning responsibility, the collaborative write model is unchanged, and `ADMINISTRATOR`, `DepartmentViewer` and `READER` are all deliberately left where they were

- 0043 — the 2026-08-27 UX pass: an additive `ux` stylesheet and script, saved views that are named URLs rather than stored rows, one parameterised `?too=tahtaeg-vahemik` so every deadline group opens exactly its own window, deferring as two existing services behind one control, and Osakond narrowed to what the team is doing, what is ahead and what is done
- 0044 — one `Arvamused` destination on the bar with the held archive as its own tab, and live suggestions under the header search: five Matter rows bounded in SQL, the existing authorized ranking reused rather than reimplemented, a request token that stops a stale answer painting over a newer one, and a plain GET form still underneath it all

- 0045 — the repeatable current-register refresh: authorship rather than existence decides whether the register may speak, a year-less date takes the sheet's year only where the snapshot agrees, a wait beside its own review is one instruction, clause ownership removes a date instead of being defeated by it, the two member-feedback counts live on the derived table with `NULL` kept distinct from a measured zero, and outreach pointers are proposed by a matcher that never writes and created only by a reviewed mapping

- 0046 — Tähtajad in two groups: the calendar week cut by the calendar and never truncated, the rest of the month behind the existing «Näita veel», the one-line pointer past it kept so nothing dated falls off the page, every group linking through the parameterised `?too=tahtaeg-vahemik` rather than a fixed name, and `WORK_DEADLINE_THIS_WEEK` deliberately left counting from today for the SEIS strip beside it
- 0047 — Arvamused as a section of the Teemad page rather than a destination on the bar: the workspace moves one level in with its Saadetud/Arhiiv strip intact, the two searches stay two searches on separate parameters (`q` and `arvamus_q`) so neither box can narrow the other's list, the section is bounded at twelve rows and states its real total, a fragment route of its own keeps the register from answering an opinion search with teemad, and `may_read_archive` is asked before anything is counted — an archive request it may not serve resolves to Saadetud rather than taking the register down
- 0048 — The v2 design implemented over the application that exists rather than beside it: no second tracking subsystem and no second statistics application, one read model serving both modes of Minu asjad, the bands redefined once in `work_items.py` with the WAIT/MONITOR semantics unchanged, `PersonalScratchpad` as the only schema change with its privacy structural rather than checked, every previous address kept as a permanent redirect, and every figure the application cannot express as a list recorded in `app/core/development_status.py` instead of drawn
- 0049 — Two department pages become one `/osakond/`: both old addresses redirect permanently with their query strings, read access stays the broad Ülevaade access the merged page inherited while Meeskond and Tehtud stay the head's and are not calculated for anybody else, one Seis strip of six figures replaces two overlapping ones, one Eesolev of five partitioning windows replaces two deadline panels over the same `real_deadlines` population with Kaugemal becoming a real list, Tehtud gains a URL-backed row-kind filter that never moves its period summary, the team table's year total and Aruandlus are made one population so one business fact has one definition, the bar carries a single Osakond destination, and Valdkonniti stays a scope of the same route — no model, no migration and no second read system
- 0050 — `Arvamuse tähtaeg` is the deadline a file carries until somebody says what happens next: an open `Järgmiseks` outranks it and suppresses it from the active-work model, with no comparison of dates — any open action wins, later date or none at all, DO, WAIT or MONITOR alike, and a register-materialised action counts like a typed one — while the stored `response_deadline` is never cleared and keeps stating itself in the Matter header; one clause in `outstanding_response_deadlines`, reader-blind because it can only remove a row, and `järgmise tegevuseta` and `Oluline tähtaeg` deliberately unchanged
- 0051 — «Uus asi» on Minu asjad: a human owner assignment gives the recipient a notice, self-assignment included, and an automated or imported one never does — the boundary being the `provenance` argument `assign_matter` already carried, now taken by `create_matter` too because that is where an initial owner is written; the notice is personal to its recipient in the sense the scratchpad is, absent from a department head’s view of that desk rather than hidden in it, acknowledged only by opening the Matter *from* the block and never by rendering `matter_detail`, superseded rather than deleted when the file is handed on or taken off every desk, and rendered as no block, no heading and no reserved space when nothing is unread — one additive model, no backfill, and no notification system anywhere else in the product
- 0052 — the simplified Teema next-action workflow: the composer stops asking a lawyer to classify their own work, `Järgmiseks` becomes its own text box beside the entry rather than being derived from it, a native step is stored `DO`/`DEADLINE`/`EXACT` with the date meaning the day it gets done, `ActionKind` and `DateSemantics` keep every stored value and no row is rewritten, `✓ Tehtud` writes no entry and swaps one row so an open composer keeps what was typed into it, superseding stays distinct from completing, and `Sildid` leaves the Teema detail page as a UI retirement with `Muu valdkond` moving into the facts block
- 0053 — the 1 September current-register snapshot: its digest is registered with the unchanged `{2025, 2026}` scope while the never-reviewed 30.08 workbook beside it is deliberately not, `küsi` joins the review verbs but only with `üle` so bare *ask* stays out of the work queue, a comma between a review verb and its particle is read as a typo when a single date and nothing lexical sits there, a review date beside a plainly-stated external milestone stays refused because 2.0 decided that on the record and a refresh brief is not where it gets reversed, and a `HETKESEIS` reading `17` resolves to no stage at all rather than growing the controlled vocabulary — parser 2.0 → 2.1, no schema change, and two 2026 rows convert that did not before
- 0054 — the action kind is not a user-facing concept: `TEEN`/`OOTAN`/`JÄLGIN` is never printed and nothing replaces it, the date meanings `TÄHTAEG`, `VAATAN ÜLE`, `OODATAV AEG`, `OLULINE TÄHTAEG` and `ARVAMUSE TÄHTAEG` stay because they describe the date rather than classify the step, `ActionKind` keeps every value and every row with no migration and no backfill so only `DO`+`DEADLINE` can still be overdue and a passed review date is still ripe rather than late, `?tegevus=` loses its three per-kind values and the two per-kind review values for one `ulevaatus` covering both, `Statistika` retires `NEXT_ACTION_BY_KIND` and merges `WAIT_REVIEW_DUE` and `MONITOR_REVIEW_DUE` into a single `REVIEW_DUE`, and `.mode`, `.modechip` and `.modeselect` are removed with the pre-v2 `work_row.html` that was their last possible renderer
- 0055 — what counts as evidence that a letter belongs to a Matter: the stopword list is written in Estonian and folded at import so intent and effect are one object — it carried `poordumine` for *pöördumine* while `fold` produces `rdumine`, a word on 207 register titles that was carrying automatic links on its own — a recipient becomes a set of named bodies over a reviewed abbreviation table rather than one opaque string, since 163 of the 192 unmatched files had a register row on their own date hidden by *MKM* against the spelled-out ministry, and a Riigikogu proceeding number becomes a route of its own, `EXACT_LAW_REFERENCE_MATTER`, corroborated by addressee or date and never sufficient alone because 25 of 165 numbers name several Matters; it runs *after* the register route rather than before it, because citation-first was measured withdrawing six links production already holds, and the rule is that more independent exact signals outrank fewer — no score, no threshold, `CONTENT_MULTI_SIGNAL` deliberately not promoted even though all 767 PDFs were measured to carry text, and `derive_links` still removes nothing
- 0056 — the opinion archive is department work product rather than a migration tool: `ARCHIVE_READERS` becomes one set asked in every authentication mode, `ROLES_WITH_RESTRICTED_ACCESS` plus ADMINISTRATOR, because ADR 0042 had already made the two lawyer roles read department-wide including every RESTRICTED Matter these letters are filed onto while a specialist still could not open the Chamber's own letter about one, and because narrowing outside the shared gate would have taken the archive from the whole department the day Cloudflare Access replaced the shared password; writing did not move at all, so reading the department's correspondence stops being a privilege while asserting what it concerns remains one, READER is not widened, an administrator reaching a letter still learns nothing about the RESTRICTED Matter it is filed onto, and the embedded section pays exactly the one extra query ADR 0047 already documented for a reader who may read the archive
- 0057 — a baseline holds the product, not the clock and not the seed: five persona images were stale on ADR 0049's top bar and are retaken from a real run's candidates with `E2E_UPDATE_BASELINES` left at `"0"`, after attribution proved each differs in one y-band and nowhere else; a search result's `source_locator` stops being a primary key, removed from the product rather than masked because `kaasamine-<uuid>` told a lawyer nothing the badge beside it did not, fixed at the producers *and* suppressed in the read model by source kind so already-stored rows stop printing one without bumping `INDEX_VERSION` — a fail-closed authorization gate that is the wrong instrument for a field in no `SearchVector`; and the folded system-run date span is masked through `.uxtl__sysrow > span:nth-child(2)`, positional because the element has no class and product markup does not carry test affordances, masked rather than normalised because the columns that differ are the digits and «näita ▸» does not move — which moves `teema-ulevaade` and `teema-1024` once, by construction, since those baselines hold the glyphs a mask now covers
- 0058 — Barlow is the typeface, not a fallback for one: FF DIN Pro sat in front of it in `--typography-font-sans` while the application shipped no `@font-face` for it, so it resolved only on machines already holding the desktop licence — the Chamber's designers saw one rendering and everybody else saw another, with nothing in the product saying which was the design; the licence is available and the answer is still Barlow, because it is self-hosted and complete at ~200 KB over latin and latin-ext, covers `õ ä ö ü` in shipped subsets, and makes a screenshot in a bug report the page the reporter saw and the 35 visual baselines mean what they claim; it was the CVI's own approved visual fallback, so this adopts a decision Communications already sanctioned rather than inventing one, and it changes nothing in CI — the runner never had FF DIN Pro, which is precisely why the drift was invisible; ADR 0009's other two typography decisions stand, self-hosting over a font CDN because a third-party request on every page view discloses who is using a confidential system and when, and the token architecture the face resolves through
- 0059 — what a `VÄLJA` mark says about an `Arvamuse tähtaeg`: the Chamber's working register writes a date for *opinion sent, work finished*, `ei saatnud` for *work finished, none sent*, and a blank cell for *still being worked on*, so the first two discharge the deadline as current operational work and the third does not — while `RECORDED_OTHER` deliberately does **not** discharge it either, which is why the test is the state set `{DATE, NOT_SENT}` and not `opinion_sent_recorded`: a cell nobody has read must not end an obligation, and leaving it outstanding is the same conservative direction `is_terminal_status` takes for an unknown `HETKESEIS`; only a `CURRENT` register row may speak and a Matter with none is never discharged, an open `NextAction` still outranks everything (ADR 0050, amended), and nothing is written — no `Submission`, no evidence, no opinion statistic, no `Matter.opinion_sent_date`, and `response_deadline` still states itself in the header; the 27-of-27 agreement between `VÄLJA` and `Submission.sent_at` is circular because all 312 opinion-derived Submissions carry `sent_date_basis = EXCEL_OUT_DATE`, and the real corroboration is 608 of 612 register/archive pairs within a day; Ülevaade's area rail stops being the one live bypass of the shared work model and the Matter header gains `täna` / `N p` / `N p üle` / date-alone (UX-010) — outstanding deadlines 95 → 18, no migration, no backfill, and the rule reverses itself the moment a newer approved workbook blanks the cell

- 0060 — deterministic document-assisted intake: once the worker has read a Matter's files, `Kontrolli dokumendist leitud andmeid` renders `Muuda teemat` with what the documents state beside it — a title from a formal heading or a subject line, a sender from the message's own `From:` or the letterhead matched in full against the organisation catalogue and its recorded aliases, a response deadline from the register parser's date scanner graded by the words around it (*hiljemalt*, *palume esitada*, the translative *-ks* make one; *jõustub*, *allkirjastatud*, *Teie 04.09.2026 nr* make a decoy), a Menetlusliik and up to three Valdkonnad from weighted cue tables keyed on stable keys, and the facts with no field — sender name and address, EIS and ministry references, links — shown with provenance and not stored; three named confidences rather than a score, `HIGH` pre-filling only an empty control, two strong answers being a conflict that pre-fills nothing, the person's own title never replaced, the form posting to the existing save so the audit trail names the person; reads only ACTIVE derivatives of current versions through `visible_to`, never opens bytes or touches the malware gate, no model, no network, no migration, and a read-only `evaluate_intake_suggestions` command that reports aggregate agreement with what people filed
- 0061 — a Matter's opinions are documents and the per-Matter Arvamused page is retired: the same letter had four surfaces and three of them were the same answer, so the opinion becomes a row on Dokumendid badged `Arvamus` — text rather than the `★ LÕPLIK` tint, naming what the file is instead of an internal lifecycle property — carrying the date it went out and its addressee, with the send's own details and `Võta tagasi` behind the row's `⋯` and drafts, `+ Uus arvamus` and `+ Registreeri saatmine` in a collapsed block that opens itself when somebody owes a draft work; what counts as an opinion gets one implementation in `app/submissions/opinions.py` — the `KODA_SUBMISSION_FINAL` role **union** the evidence bound to a SENT Submission, deduplicated by document and `visible_to` on both sides, because `select_final_evidence` deliberately never reclassifies a file and the role alone had a Matter saying `Arvamust ei ole lisatud` beside its own sent opinion (UX-005); the stored role, `Submission` as the canonical send, the sent-evidence constraint, the exact `final_version`, the SHA-256, `OpinionSubmissionImport` and every ChangeEvent are all untouched — the checksum, the byte size, the version number and the archive importer's match reasoning simply stop being printed under a letter somebody is reading; the rail becomes filenames and nothing else, `register_sent_opinion` composes the three existing services rather than restating them, `Seotud arhiivikirjad` moves to Dokumendid under the same `may_read_archive` gate, the old address redirects after checking authorization so a bookmark still works and a stranger still gets 404, ADR 0047's standalone Arvamused workspace stands unchanged — and there is no schema migration, no data migration and no search rebuild — **amended 2026-09-26**: the addressee rule the 2026-09-11 amendment stated for `Registreeri saatmine` now holds for every new interactive send, so a draft row's `Märgi saadetuks` asks `Adressaadid` (opening on the draft's own) and `mark_submission_sent_on_open_matter` refuses a send with none under the row lock with nothing written, while drafts stay free, archive sends with no resolved addressee stay readable history and the correction form keeps its requirement (ENG-041)
- 0062 — Seotud materjalid: a Matter page section that proposes related Matters, earlier opinions and archive letters and lets the lawyer confirm, choose or dismiss — suggestions are derived on request from the existing search and archive projections with no index-version change and no rebuild, ranked by named deterministic weights where the same act or a similar title clears the threshold and the same area, ministry or track alone never does; three additive tables hold only what a person decided, `MatterRelation` being the approved symmetric `RELATED_TO` slice of the specification's typed `MatterRelationship` with no type column, background material being neither an `OpinionArchiveMatterLink` nor a moved `Submission`, and dismissals being durable Matter-level preferences; authorization runs before ranking, including the deduplication of an archive letter against a Submission the reader can actually see; nothing is linked automatically, GET writes nothing, and the section is one quiet row until opened
- 0063 — one Organisation catalogue behind Saatja and Adressaat: the department asked for one place holding institutions and architecturally there already was one — `Organisation`, pointed at by both `Matter.source_organisations` and `Matter.addressee_organisation` — so nothing migrates and the two relations stay semantically distinct, *who it came from* and *who it is answered to*, never merged; what made it feel like two databases was that Adressaat accepted a typed name while Saatja refused one and said so on every surface, and the workflow that sentence prescribed (leave the Teema, add the body under Asutused, come back) was not performed — people filed the Teema with no sender, so the register lost the fact rather than gaining a considered one, which amends ADR 0025's "creating a Matter is never a way to create an institution"; `Uus saatja` now resolves through the same `resolve_organisation_name` the addressee half uses — normalised-exact reuse, creation only of a genuinely new body, refusal of a spelling that already names two, inside the save's own transaction on `Uus teema`, `Muuda teemat` and `Saabunud` — and because the sender relation is plural a typed name is *unioned* with what is ticked rather than winning over it, one sender for a body reached twice; the quick shortlist stops collapsing to a single chip on a young dataset by being *filled* to eight from sender usage, then addressee usage, then the catalogue alphabetically with both usage passes scoped by `visible_to` so a restricted Matter can neither move a chip nor disclose a body, and the «Vali nimekirjast» disclosure is gone with the search over the whole catalogue now on the page at rest above a height-capped scrollable list — no schema change, no migration, and the search box still posts nothing so a half-typed «Kliima» cannot become an institution
- 0064 — assisted intake happens while the Teema is being created: ADR 0060's reader is right and its *place* was not — six steps to be offered a deadline back on a different page after the record had already been made from whatever could be typed before reading anything — so choosing a file on `Uus teema` stages it, the extraction worker reads it, and the suggestions land on the form that is still open; ADR 0060 rejected exactly this and both of its objections were about parsing *in the request*, so every one of them is kept: `parse_source` is the orchestrator's pure half factored out unchanged and is the only parser stack either surface has, `_run` still publishes derivatives, fragments, attachment Documents and the search projection while a staged file publishes a little JSON on its own row, and the staged queue is a real queue with `SKIP LOCKED` claims, a fence token and three clauses — removed, consumed, expired — that keep it bounded; two additive tables hold one person's unfinished form with an owner, an expiry and a consumed stamp and nothing else, because a hidden Matter would be a row every list had to filter out and a nullable `Document.matter` would be a canonical row the authorization model assumes cannot exist, with the bytes in the held-uploads storage class under an `intake/` prefix that `pending._sweep` provably cannot reach; `analyse()` did not change at all — one more `AnalysisInput` builder, the same budget arithmetic, the same confidences, the same conflict rule and literally the same candidate template — and the one thing that could not be reused is *how* a pre-fill is applied, because the form is already on screen: `prefill_initial` still decides and the browser applies it only to a control that is empty and untouched, treating one still holding exactly what it last wrote as still empty, so a later answer may replace an earlier answer and never a person; `Loo teema` remains the only thing that creates anything and promotes the exact staged bytes — rehashed against the checksum recorded at upload, one file one Document, extraction deliberately re-run rather than promoted so canonical derivatives keep one producer — while later uploads to an existing Matter reinterpret nothing and `Kontrolli dokumendist leitud andmeid` stays as the explicit re-check; reading is never a gate, the states a person sees are «Loen faili…» / «Loetud» / «Ei saanud lugeda» / «Sisu ei loeta», and `prune_intake_staging` is a command an operator runs and nothing schedules
- 0065 — adding a structured fact happens on the Matter: `+ Jõustumine` and `+ Töövõit`, in both the empty-state chip and the populated-section forms, open an accordion under the area the record lands in instead of navigating to a page of their own, because a commencement is a kind, a date and a sentence and the trip to write it was most of the interaction — and worse on a refusal, which arrived on the separate page the person then had to leave again; one route, one form, one service and two renderings, with `HX-Request` the only switch, so a bookmark, a deep link, a browser without scripting and the write-boundary suite all still get the standalone page and the redirect they always got, and `EffectiveDateForm`, `WorkVictoryForm` and the two `add_*` services are untouched; the accordion is the render rather than a state machine — the block carries at most one open form, so opening Töövõit *is* closing Jõustumine, and opening, closing and `Sulge` write nothing — while a refusal comes back as 400 with the values still in the boxes through the htmx response handling `static/js/app.js` already configured; ADR 0018's refusal of *«HTMX fragment swapping for the capture forms»* is superseded only this far, since nothing in `app.intelligence` reads `app.matters.views`' context builders and what a fact section may now do is re-render *itself*; `Muuda`, `Tühista`, `Kinnita töövõiduks` and `Ei realiseerunud` deliberately keep their own pages, `Oluline tähtaeg` stays with the composer, an `auto_id` rather than a form prefix keeps the composer's own `kind` from stealing the commencement radios' labels while leaving the posted field names identical — and there is no new route, no new write path, no migration and no additional query on an ordinary Matter
- 0066 — the malware scan gate is a real scanner: ADR 0014 drew the line — no parser opens a binary until a scanner has cleared it — and enforced it correctly, but nothing in the application ever wrote `CLEAN`, so on the real-data stack the gate was not a gate but a wall: `Uus teema` staged a file as `PENDING`, the extraction queue permanently excluded it, and the page said «Loen faili…» to somebody who waited half an hour for an answer that could not arrive; the rehearsal could not see it because it runs `REAL_DATA_ALLOWED=0`, where `PENDING` is already extractable, so it exercised a branch production could not reach, and neither could a suite of four thousand tests that never ran assisted intake with real-data mode on; fixed by building the scanner rather than weakening the gate — every easier answer (letting `PENDING` through, stamping `CLEAN` at upload, reading a missing scanner as `SKIPPED`, parsing in the request or in the browser) would have made the sentence «a scanner examined these exact bytes» false while every reader of the column went on believing it — so ClamAV runs over clamd's own `INSTREAM` protocol, implemented in `app/documents/scanning.py` rather than taken as a dependency, in a pinned internal container that mounts *nothing*: the bytes are streamed to it from wherever they already live, so a container holding a signature database and a parser for every archive format ever invented never sees the evidence tree; four transitions and only four — clean, a signature, an error on the file, and an unreachable scanner leaving the row exactly where it was, because a scanner that is down has said nothing and nothing is not a clearance — with `interpret()` written once so a `DocumentVersion` and a `MatterIntakeFile` cannot come to mean different things by the same `MalwareScanState`; real data with no scanner configured refuses to start (`juristid.E015`), deliberately a check on *configuration* rather than on availability, and the healthcheck asks whether clamd is *loaded* because a clamd with an empty signature database answers a socket perfectly and calls everything clean — which is why CI streams the EICAR test file at the real image and requires `INFECTED`, and why the rehearsal stack now runs the scanner too: the two stacks differ in their data, not in their security model
- 0067 — `Uus teema` asks two questions about one catalogue, and creates real work: three reported complaints turn out to be one observation, that the form asked its two counterparty questions as though they were about different things — Saatja was chips plus a permanently visible search over a permanently visible scrolling catalogue while Adressaat was chips plus «Vali nimekirjast (N)», so a person learned one interaction for the field on the left and another for the field on the right; the permanent catalogue is withdrawn because the shortlist is *filled* to eight from the bodies this department actually works with and answers the question on almost every visit, so what it bought was a search box occupying the Saatja column every single time — but the half of that round which was load-bearing stays: `Uus saatja` remains *outside* the disclosure, because the answer to «the body I need is not on this page» must not itself be behind a click or the workflow ADR 0063 removed comes back; a sender chosen on the form is promoted to the front of the Adressaat choices and is **never** selected — ordering is a suggestion and a counterparty is a fact — done server-side for the bound form and in the browser for the live one, with an addressee somebody picked by hand surviving any subsequent change of sender, and the promoted option *moved* rather than copied so the radio group still holds one control per organisation; a sender being typed for the first time is offered as a chip standing for a name rather than a record, filling the existing `addressee_name` path, and on save one typed sender and one typed addressee resolve against one catalogue inside one transaction so the same spelling becomes one `Organisation` row used twice; and `Andmeklass` leaves the create page entirely — the field is removed from the form rather than hidden, so a forged `is_test_data=on` creates REAL work, while `Matter.data_class`, the enum, the existing TEST records, the REAL/TEST reporting filters, the purge tooling and the rail's after-the-fact «Märgi testandmeteks» are all untouched — with the freed column going to Saatja; no schema migration, no data migration, no search or archive version change
- 0068 — the operational snapshot records which step it photographed: `capture` copies a `NextAction`'s kind, date meaning and date onto the snapshot row and does so unauthorized on purpose, but `visible_snapshots` scoped those rows by the *Matter's* visibility alone, so a step restricted below a NORMAL Matter had its three facts published to every reader of that Matter — a projection broader than its source, F-4 of the 2026-09-09 restricted-data leakage audit and the one finding it left open; the read now blanks the three columns rather than dropping the row, because a Matter leaving a trend is itself the disclosure, and a blanked row is deliberately the same shape as a day with no next action at all; what to blank is decided from a new nullable pointer to the step, asked of `child_visibility_q` against the **live** row, never from a stored copy of the step's visibility — which would go stale in the fail-open direction the day somebody restricts a step after the photograph was taken; the blanking is SQL rather than Python because the first surface to read this table will be a chart; one migration adding one nullable foreign key, no data migration and deliberately no backfill
- 0069 — on `Uus teema` the Saatja is the Adressaat, and Adressaat folds away: the two counterparty questions have the same answer almost every time — a ministry sends a draft and the department answers the ministry — and ADR 0067 acknowledged that as far as *ordering* and then refused the next step, on the argument that offering costs a click while guessing costs a wrong counterparty discovered late or never; the risk is real and the trade was not, because the click was never the cost (the cost is scanning a second chip group over the whole catalogue for a word you have just typed, possibly behind a second door) and «discovered late» stops being true once the field states its own value on every visit — so a single unambiguous sender now answers `addressee_organisation` by primary key, a sender being *typed* answers `addressee_name` and both halves resolve through one catalogue in one transaction so the spelling becomes one `Organisation` row used twice, and a typed spelling the catalogue already holds answers with the row rather than the word; the default is not a lock — an answer given by a person wins for ever and is carried in a hidden `addressee_is_manual` rather than inferred from which chip is first, since moving chips is what this feature does, while a default that came from a sender is taken back when that sender goes and several senders produce no default at all (the browser keeps the *first* sender as its seed, a POST is an unordered set, so the server's rule is one-sender-defaults and two-or-more-do-not); the whole control folds behind the existing `<details class="chipdetails">` summarising as «Adressaat» or «Adressaat · Kliimaministeerium», opening for a click and for an error on Adressaat itself but deliberately never for the default, since a page that unfolded a section because it had answered a question would be reacting to its own writing; the rule is the *server's* and the derived value is written into the bound data rather than only into `cleaned_data`, so a POST with one sender and no addressee saves correctly with no JavaScript and a refused save re-renders showing what it will file — `Muuda teemat` and `Saabunud` are untouched, `_promote_selected_senders` survives as `_promote_named_senders` because the collapsed summary makes the shortlist the only place a default can be seen, and there is no schema migration, no data migration and no search or archive version change
- 0070 — `Õigusakt` is a canonical Matter field and it is not Menetlusliik: the register's `ÕIGUSAKT` column has been read and stored raw since Stage 2A under `authority = "deferred"`, on the correct argument that nobody had said where it belonged, and the department has now said — `Menetlusliik` answers *what kind of procedure*, this answers *what kind of instrument*, and `ELi õiguse ülevõtmine` about a `Seadus` is an ordinary combination neither field can derive from the other; it is **multi-select** because the source genuinely says several (`S, M`, `direktiiv ja määrus`, `direktiiv+määrus`, `direktiiv/määrus`) and a single value would have to concatenate or discard, so the control follows the model per ADR 0025 — checkbox chips with the count and the clear marks that Menetlusliik directly above deliberately has neither of, which is the whole of what stops the two rows reading as one question split in two; the vocabulary is seventeen reviewed rows of a third governed taxonomy `LegalInstrumentType` — not `Track`, not `Tag`, not `PolicyArea`, not a comma-separated `CharField` and never grown from what somebody typed — derived from a read-only survey of all sixteen year sheets that found **2418 non-empty cells in 58 distinct spellings**, collapsed by an alias table in which EU-ness is part of the instrument only where the EU act is legally distinct (`EL määrus`, `Direktiiv`, `EL teatis` stay their own; `EL strateegia` is `Strateegia` and `EL konsultatsioon` is `Konsultatsioon`, because whether the *procedure* is European is Menetlusliik's answer already); one seam `canonical_legal_instrument_keys` splits combined cells before normalising and refuses to map half of one, and two spellings — `EL` and `sisendi küsimine VTK ettevalmistamiseks`, seven cells of 2418 — are left **unmapped rather than filed as `Muu`**, because `Muu` is an answer and neither of them gives one; `Muu` *is* a vocabulary row here unlike Valdkonnad's, since 1130 historical cells say exactly that and a vocabulary with nowhere to put them would lose the department's own answer, and it reveals `Õigusakti liik` — required while ticked, cleared when not, creating no row ever; `legal_instrument_raw` is untouched and stays what the spreadsheet said, so the era contracts move to a new and honest `mapped` authority (a reviewed home *and* a reviewed reading that the importer still does not write) at contract schema 1.1 — and there is no backfill, no import, no register-refresh field, no search-index change and no production access
- 0071 — Teemad is the discovery surface and «Tähtajad» is not a destination: the bar's fifth item opened three generated department-wide pages — Olulised tähtajad, Jõustuvad aktid, Töövõidud — each a list of Matters selected by one property and presented as a place to be, so a reader had to know in advance which of four destinations their memory of a file belonged to and none of them could be intersected with the register, which is why «avatud teemad, millel on töövõit» was a question the product could not ask; `Töövõit` and `Jõustumine` become structured filters on Teemad (`?toovoit=` taking `on`, `puudub` or a business year read through the imported `VISIBLE_VICTORY_STATUS`, `?joustumine=` over the active `MatterEffectiveDate` rows and never inferred from a title, a Menetlusliik, a Hetkeseis or a tag) with `?joustub_alates=`/`?joustub_kuni=` a **containment** window rather than an overlap one — III kvartal 2026 does not answer 01.07–31.08 because it may commence in September, which is `_direction_q`'s own rule with both ends named — while `GENERAL_ORDER` and `UNKNOWN` carry no date by database constraint and are found by `?joustumine=on`; an `Oluline tähtaeg` needed no code at all because the read model has always given it to the Matter owner, and needed proof instead, asserted on `/minu-asjad/` itself rather than through `work_items`; both filters are one correlated `EXISTS` over a child table scoped to the reader *before* the existence test contributes anything, so a restricted child makes its Matter neither appear under `on` nor disappear under `puudub` and a file that won three times is still one row; every retired address resolves in one hop and the old windows (`?suund=`, `?allikad=`, `?aasta=`, `?staatus=`) are dropped rather than approximated, because they described a list of facts and the register lists Matters; two Statistika figures stop counting facts and count Matters through `register_population`, captioned for what they now count, since the lists they opened are gone and a number must open the list it counted; and the reported Asutus-pool defect is fixed in the same surface — never a second catalogue, but a twenty-row alphabetical first page feeding all three institution controls when only one of them had a search box, so a body outside it could not be chosen from `Saatja` or `Adressaat` and an applied one was not redisplayed in its own control, silently dropped by the next submit — with `asutus=puudub` and a legend that renamed itself on first use fixed alongside; no schema migration, no data migration, no search-index change, and not one `MatterImportantDate`, `MatterEffectiveDate` or `MatterWorkVictory` row touched
- 0072 — document reading is an ephemeral assisted-intake capability: on 2026-09-10 a single-row `INSERT` into a 264 kB audit table blocked for two minutes and gunicorn killed the worker holding it, because the extraction worker — newly able to read the `PENDING` backlog ADR 0066 had just unblocked — was draining 16 000 historical attachments into text fragments, derivative files and search rows on a parity-protected array whose parity device is a saturated USB disk, taking checkpoint `fsync` from under 2.5 s to 154.9 s; nobody had asked for that corpus text, and the capability the department did ask for is one small file read once while somebody watches a form, so the persistent pipeline is withdrawn and reading becomes what it is for: a dedicated `intake-reader` service whose universe is `MatterIntakeFile` rows on a live staging session and which has no import path to `DocumentVersion` at all — asserted structurally against the module's own imports, and behaviourally against ten thousand pending canonical rows beside one staged file, of which it claims exactly none; the whole malware subsystem goes with it — `scanning.py`, `check_malware_scanner`, `juristid.E015`, the `MALWARE_SCANNER_*` settings, the `clamav` service on both stacks, the EICAR CI job and the topology guard — because a gate in front of a parser that no longer runs on the corpus is a container, an outbound updater and a start-up check for a threat never observed here, while the size ceiling, the extension allowlist and the content-signature check that were always doing the real work stay exactly where they are; `extraction_state` gains a fourth terminal value, `INTAKE_READ`, so a file promoted by `Loo teema` is never handed back to a queue merely because a Matter now exists to hang it off — ADR 0064's refusal to *carry* a staged parse across is untouched, what stops is performing the second one — and the honest cost is stated on the Statistika tab rather than hidden: a document filed through `Uus teema` has no permanent text and is not searchable by content, which is a decision and not tegemata töö; `extractor` leaves both Compose files so `up -d` cannot restart the workload that caused this, `Pealkiri` gains a pre-fill on `Uus teema` and only there, and the reader gains an evaluation corpus of twelve invented envelopes scored correct / missed / **wrong** — 41 correct, 3 missed, 0 wrong, precision asserted absolutely and recall as floors
- 0073 — on `Uus teema`, one control finds an institution and names a new one: Saatja offered three ways to answer one question — a row of eight quick chips, a «Vali nimekirjast (N)» disclosure holding a search box and the rest of the catalogue, and a separate `Uus saatja` text field outside it — and Adressaat offered the same three nested one fold deeper, so a person had to decide which of the three they were on *before* they had looked, which is exactly backwards; each affordance was argued for correctly about the thing it was about and three correct affordances are not a correct control, so both fields become one box reading «Otsi või lisa asutus…» with a `+` attached to it, and the disclosure and the separate new-name box are gone from the scripted page; the distinction ADR 0063 protected with two boxes — «one control filters what exists, the other says this is a body you do not have» — is a distinction between two *intentions* and is now carried by an *action*: the box has no `name` and posts nothing, `+` moves what was typed into the `sender_name` / `addressee_name` fields that have always carried a typed institution and draws a dashed provisional chip, and only `Loo teema` creates anything, still through `resolve_organisation_name` inside the save's own transaction with normalised-exact reuse, alias reuse and refusal of a spelling that already names two; `+` on a spelling the catalogue already holds *selects that body* — feedback in the browser, fact on the server — and every institution is rendered as a real form control with only the shortlist on screen, so the search reveals a chip rather than describing a row and a body chosen through it, returned by a refused save, or ticked by the intake reader is a selected chip with no round trip; an answer is never hidden, cardinality is unchanged (Saatja 0..N checkboxes, Adressaat 0..1 radios), ADR 0069's fold, summary, sender→addressee default and manual override are untouched, ranking stays presentation with no usage count rendered and both passes scoped by `visible_to`, aliases reach the browser as pre-normalised `data-aliases` in one query, Enter takes the highlighted result and never adds or submits, a `<noscript>` block keeps the old pair for a browser that cannot search, `sender_control.html` is unchanged and still renders the previous shape on `Muuda teemat` and `Saabunud` because their interaction contract is not identical and they were not part of this review, and there is no schema change and no migration
- 0074 — the Teema detail page is the approved target: the 2026-09 refinement rebuilt this page against a specification that had by then been superseded, and this is the approved design — `TEEMA_TARGET.html` and its spec — implemented against the existing domain rather than over it; `Saabus` moves into the header metaline at position 4 so arrival and response deadline are read as the pair they are, and `Tähtaeg` narrows to `Matter.response_deadline` because a Riigikogu reading in three weeks is not a day Koda owes an opinion on; the composer is **open by default**, reversing ADR 0043 — the target makes the form short enough to live open rather than asking for a click before anything can be recorded — with `+ Manus` retired for an always-visible file affordance, and five progressive panels over one `Salvesta` and one transaction, of which `+ Kaasamine` reverses ADR 0031 (that decision was sound while the standalone section existed; the section is gone, so this is the one entry point rather than the second) and `+ Jõustumine`/`+ Töövõit` come from the retired facts panel, with a win now recordable without closing the file; **closing a Matter is not a claim that an opinion was sent** — the panel asks `Kuidas lõppes` and an optional `Lõppsõna`, and the sent opinion, its date, its recipients and the victory decision go, because requiring the PDF made the commonest closure impossible to record honestly while every canonical rule they protected is unchanged and still proven through `compose_update`; the standing facts panel and the standalone `Kaasamine` section are removed and their four record types projected into a new two-kind chronology — a 12px accent dot for what happened to the file, a 6px muted one for work somebody did on it — off the canonical rows rather than duplicate audit events, superseding ADR 0065's presentation clause; a derived `.tl-strip` above it reads where the file stands from those same scoped facts with no new table, drawing only milestones that exist and never `Loodud` for an imported row whose `created_at` is a fact about a migration; one additive migration adds `EngagementKind.MEETING` (reversing ADR 0027's constraint) and a nullable `response_count` with no backfill, because «nobody answered» and «nobody counted» are different facts; the rail is four blocks with `Muu valdkond`, `Andmeklass` and `Märgi testandmeteks` retired from this page only and `Märkmed` autosaving without a button; the prototype's stale `Tähtajad` shell is **not** adopted and the 420px drop-zone overlap its own spec calls a defect is fixed rather than reproduced; no production migration, no backfill, no search-index change; amended 2026-09-26 (ENG-021): §20's «still reached from a work row elsewhere» was untrue — no page posted to `defer_action` or `review_action` — and the review half is reachable again as `Vaatasin üle` beside a waiting step (ADR 0075's amendment)
- 0075 — the Teema current action and add-to-matter workspace: the upper page becomes two clearly separate zones, superseding 0074's open composer and the `Järgmiseks` row above it, because that pair left the commonest act on the page — finishing the task the page is telling you to finish — with no operation of its own (`✓ Tehtud` wrote no description, the composer completed no step, two saves in either order with nothing tying them together) while its one `Salvesta` could mean any of seven things depending on which boxes happened to carry a value; `PRAEGUNE TEGEVUS` shows the open `NextAction` and asks exactly one question, `Mida tegid?`, and **saving that result is what completes the step** — one atomic operation over `add_entry` + `capture_supporting_evidence` + `complete_next_action`, no `Märgi tehtuks`, no second confirmation, the description required because a step marked done with nothing said about it records only that somebody pressed a button, and a file never standing in for it; the form carries the exact `action_id` it was rendered against and the service re-reads the open action under the Matter row lock and refuses anything else, which is what stops a stale tab from completing the task that replaced the one it is showing and makes a double submit produce exactly one completion; a successful save opens no new step and a Matter with no open step renders one compact line and no textarea, because *no current action* and *a blank completion form* are different states; `LISA TEEMALE` is the other zone — seven choices (`+ Märge`, `+ Järgmine tegevus`, `+ Kaasamine`, `+ Oluline tähtaeg`, `+ Jõustumine`, `+ Töövõit`, `+ Lõpeta teema`), not seven forms, each opening one small form with its own fields, validation, `Salvesta`, endpoint and atomic operation, one panel open at a time, and `+ Järgmine tegevus` absent while a step is open because `Muuda` beside the task is the one control for the one open action; files stop being a single Matter-level attachment and support an **exact record** through one additive `DocumentLink` table — five nullable typed FKs with an exactly-one `CHECK` and a partial `UNIQUE` per target, the shape `related_materials` already uses, chosen over a `GenericForeignKey` that has no referential integrity and over a composite foreign key that would need redundant unique constraints on six production tables and raw SQL, with the same-Matter invariant enforced at `link_document_to_record` and reported by `check_evidence_integrity` as `cross-matter-link`, and a link readable only when **both** its ends are; the chronology shows those files on the row the record already draws and gains no dot, no row and no count for them; `compose_update()`, `ComposerForm`, `matters:compose`, `matters:complete_action` and `next_action_row.html` are all left available as compatibility surfaces rather than deleted; one additive migration, no backfill, no fabricated associations for historic files, and no change to the header, the rail, the process strip, the chronology grammar, the search index or the extraction topology; amended 2026-09-26 (ENG-021): `PRAEGUNE TEGEVUS` offers `Vaatasin üle` beside a `WAIT` or `MONITOR`, posting to `review_action` → `acknowledge_review`, and Minu asjad's «Vaatasin üle…» lands on it; `Muuda` keeps the replaced step's kind and date meaning as it keeps its precision, while new work stays `DO` / `DEADLINE`; a review that would change nothing writes nothing; `defer_action`, `complete_action` and `next_action_row.html` remain unreachable compatibility surfaces; whether a deliberately chosen new date in `Muuda` turns a review into a plan is an open owner decision
- 0076 — a closed Matter's Dokumendid surface is read-only for business work: 0075 §12 stated that a closed teema accepts no new canonical content and enforced it under the Matter's own row lock, but only on the Teema tab, so integrated QA on `integration/post-qa-sep11` posted to the file list of a Matter somebody had already finished and found three doors still open — `↑ Lae dokument` writing a `Document` and an immutable `DocumentVersion`, `+ Uus arvamus` writing a DRAFT `Submission` with its recipients, `+ Registreeri saatmine` writing a SENT one with its send event — and a fourth on any draft left behind, where `Lisa fail`, choosing existing evidence and `Märgi saadetuks` all still advanced it; none of that was a regression and all of it contradicted the rule enforced on the tab beside it, so the product decision is stated plainly: a closed Matter is read-only for **normal interactive business work**, a lawyer may read it, browse its chronology, open and download its evidence and inspect its opinions, and if ordinary legal work has to continue the Matter is reopened first, worked and closed again — `closed → no normal business writes`, never `once closed, forever immutable`; the rule is **not** put in the primitives, because `create_document`, `add_evidence_version`, `create_submission`, `select_final_evidence` and `mark_submission_sent` are the same services the register import composes and most of the Matters `app.legacy_import.opinion_apply` files two and a half thousand historical letters onto are shut, so a guard down there would refuse the historical record and protect nothing, and it goes instead into seven thin interactive use cases above them (`capture_evidence_on_open_matter`, `add_version_on_open_matter`, `create_opinion_draft_on_open_matter`, `attach_final_evidence_on_open_matter`, `select_final_evidence_on_open_matter`, `mark_submission_sent_on_open_matter`, `register_sent_opinion_on_open_matter`) — the shape `record_engagement` already had over `add_engagement`, with no `allow_closed=True` escape hatch and no role or admin exception, because import privilege comes from using the import path and not from being a powerful user of the UI; `Registreeri saatmine` is the one that had to be decided rather than assumed and it is **refused**, so the two layers answer the same call differently on purpose — the service files a letter Koda really sent in 2019 onto a Matter closed in 2019, while a person in front of the file creating a canonical `Submission`, its recipients and its `SUBMISSION_SENT` event on a finished Matter with a date they typed is ordinary business work with a backdated field; the fresh page renders read-only behind `can_add_content` (`can_write` **and** `matter.is_open`) with the closed banner's `Ava uuesti…` as the way out, but hiding a control decides nothing and the boundary is the lock — each use case takes the Matter row `FOR NO KEY UPDATE` and reads `is_open` from the locked row, so the stale-tab race has one outcome, proven against real PostgreSQL in both orderings including the upload, whose stored object is written before the row that describes it and would otherwise be an orphan no constraint can see; historical correction (`Võta tagasi`, supersession, editing a recorded fact), SharePoint working-document references and personal `Märkmed` are all explicitly outside this boundary and unchanged; no schema change, no migration, no backfill and no `INDEX_VERSION` move
- 0077 — a personal note's autosave cannot overwrite a version it never saw: `MatterPersonalNote` and `PersonalScratchpad` are the only writes in this product that file no `ChangeEvent`, which 0074 §18 and the Teema redesign §22.4 argued deliberately and which still holds — an autosave of somebody's scratch paper is not a business change and recording every keystroke pause as authoritative history would bury the history it sits beside — but adversarial QA on 12 September found the consequence nobody had written down (QA-09): tab A saves a note, tab B, open since before that save and still holding the older text, autosaves 900 ms after its next keystroke, B's copy replaces A's, and because this write leaves no row saying what changed **nothing anywhere** holds the version that was overwritten; every other overwrite in this application is recoverable precisely because every other write files history, so the absence of it is what turns an ordinary last-write-wins into persisted data loss, and two tabs on one Teema is how a lawyer reads a file while writing about it; so a rendered editable note carries the version it was filled from and a save whose version is not the stored one **writes nothing** — the token is `updated_at` rather than a column of its own (`auto_now` moves it without anything remembering to, PostgreSQL keeps it to the microsecond so two saves cannot share one, and it is already what the rail prints as `Salvestatud HH:mm`, so the fact the reader sees and the fact the server compares are the same), a note that has never been saved has the empty token and that is a version like any other, so a second tab whose box was empty when it loaded cannot silently replace a note the first has since created; the services read the row back under `select_for_update(no_key=True)` — `no_key` for the reason every lock in this codebase takes it — and the refusal answers **409**, which `app.js`'s `htmx:beforeSwap` lets swap for the same reason it already lets a 400 swap, because a dropped response leaves the box looking exactly as though the save had worked; what comes back is **not the textarea**, so the person's own words stay where they are with caret and selection intact, and what arrives is one sentence in the hint slot plus the newer version out of band, read-only in a `<pre>` they can copy from — it does not merge (nobody can tell from two versions of a private note which sentences were meant to survive), it does not choose, and it does not adopt the newer token, because handing that back would mean the next keystroke overwrites what the other tab saved, the defect with one more step in it; `expected_revision=None` means «no opinion» and exists for `Uus teema`, which creates a Matter and its first note in one act where there is no earlier version for a second tab to hold; **scope is these two pads and nothing else** — canonical business editing is not given optimistic locking here, that is a much larger contract with a real design question about what a conflict means for a record that files history and can be corrected, and the reason these two come first is not that they matter most but that they are the only writes where an overwrite leaves no trace; no schema change, no migration, no backfill
- 0078 — `LISA TEEMALE` is a stable choice bar, and `+ Kaasamine` asks for its dates: two findings from using the workspace on real files, decided together because they are the same panel. The seven operations were `<details>` whose summary is the chip, and an open one took `flex: 1 1 100%; order: 1` — so the element that grows *was* the element you press, the chip somebody had just clicked jumped to the head of the next line, and every chip after it shifted along; no `order` value repairs that, so the control and the form become two elements: a clipped radio, its visible `<label>` chip which is `flex: none` in every state, and the `.cx-panel` form, which is the only item that ever takes `order: 1` and a full-width basis and therefore lands on its own line under the whole row — one radio `name` hands «only one open at a time» to the browser, `:checked` paints the active chip in the existing accent tokens and reveals its form, the whole contract survives with scripting off (the server renders `checked` where it rendered `open`), the radio is clipped rather than `display: none` so the group keeps its arrow-key contract and the focus ring is drawn on the chip, `aria-controls` names each form, `role="tablist"` was rejected as owing a keyboard contract a radio group already has, and `ux.js` keeps exactly one behaviour — clicking the active chip un-chooses it — while `Muuda` in PRAEGUNE TEGEVUS stays a `<details>` because it is alone on its line with nothing to displace; second, `+ Kaasamine` asked for no date and the view passed `timezone.localdate()` on every save (0074 §9), so a consultation held in March and typed up in September was filed as a September consultation — a false fact written by the server with no box on the screen to contradict it — which is superseded by a visible optional `Kaasamise kuupäev` **pre-filled with today** (the common case is real and retyping today is the friction people complain about; what changes is that the default can be read, changed and emptied, and an emptied box stores `NULL`, «kuupäev teadmata», with nothing putting today back); third, `Tagasisidet ootame kuni` — one new nullable `MatterEngagement.feedback_deadline` recording what was asked *of other people*, deliberately inert: no `NextAction`, no `MatterImportantDate`, not `Matter.response_deadline`, no work item, no overdue badge, no count, no filter, no index, because what is owed by this office is work and is already modelled while a reply-by date asked of a ministry is a recorded fact, and a column that generated a task would make every historical consultation overdue on the day it is entered — optional and undefaulted (today is never a plausible reply-by date), a past deadline accepted, the one refusal being a deadline *before* its engagement, reported on the deadline field; shown on the engagement's own chronology row when set and saying nothing at all when not; the migration is one additive nullable `DateField` with no database default, no `RunPython` and no backfill, and `update_engagement` takes the field behind the existing `_UNSET` sentinel so every caller that predates the column leaves it alone; date precision, an engagement-correction UI, a response-obligation model, a feedback-deadline work item, a deadline filter and any new statistic are all explicitly left open
- 0079 — a planned date is recorded at the precision it is actually known to (superseding ADR 0052 §4 and the `EXACT` half of its §3, and widening ADR 0074 §11): `DatePrecision` and `app/workflow/dates.py` were already right — *II kvartal 2026* stores as `2026-04-01` + `QUARTER` and prints through `format_at_precision` — but a lawyer could not *say* it (only `Oluline tähtaeg` offered a precision control, and it stopped at `Kvartal`; `Järgmine tegevus` and `Jõustumine` took a day, `Töövõit` took no date at all), and where a period *was* stored the four lateness queries and `NextAction.is_overdue` compared its ordering anchor against today, so a step planned for *september 2026* was reported late on 2 September and carried «30 p üle» on 1 October; so `Täpne päev` / `Kuu` / `Kvartal` / `Aasta` are offered on all four Teema facts through one composer normalising to `bounds_for`, the anchor stays the first day of the period and stays internal (no surface prints it as a day), a `DO` + `DEADLINE` step becomes late only once its whole period has ended — *september 2026* on 1 October, *2027* on 1 January 2028 — with `days_late` counted from the period's last day, and `app/workflow/lateness.py` states that once in both readings the product needs, a Python predicate and a `Q`, because the four surfaces that count lateness each had their own `target_date__lt`; which combination can be late is unchanged and `due_for_review` deliberately keeps its own boundary a month earlier (a review date is a reminder, a deadline is a promise); `HALF_YEAR` and `INFERRED` stay stored, rendered and correctly compared but are not offered for new input, and an unrelated edit never coerces them away; `Töövõit` now asks for an explicit period with no today and no current-year default, while existing undated rows keep none and are **not** backfilled; an approximate anchor that happens to equal a Matter's `Arvamuse tähtaeg` does not suppress PR #208's secondary official-deadline line, because anchor equality is not fact equality; `Arvamuse tähtaeg`, `Saabumise kuupäev`, `Kaasamise kuupäev`, `Tagasisidet ootame kuni`, submission send dates, document dates, closure dates and `Entry.occurred_at` all stay exact; no schema, no migration in either implementing pull request, and no data migration anywhere
- 0080 — `Õigusakt` is read from one document's head, not pooled over the envelope: ADR 0070 made `Matter.legal_instruments` canonical and put its control on both forms, and left a lawyer retyping on `Uus teema` a classification written on the front page of the file they had just dropped onto it; the deterministic reader already reads that page for five other fields, but its pooled scoring — right for Menetlusliik and Valdkond, which are properties of an envelope — would hand the answer to whichever document is longest, and on a legislative bundle that is the comparison table naming the transposed directive in every row; so each readable document is classified on its **title block** alone (the run of lines before a salutation, a sentence, a verb of sending or a numbered section, blank lines deliberately not load-bearing because `parse_source` removes them), the envelope picks one speaker — a document's own head over a message's subject over an annex — and only the speaker's own kind may reach HIGH while everything else corroborates at MEDIUM; a specific kind always beats the generic `Eelnõu` and the two are never offered together, `Muu` is never inferred because it would make `Õigusakti liik` required with nothing honest to write in it, `ANNEX_ONLY_HIGH_MARGIN` keeps an annexed `määruse kavand` at «Kasuta» beside a parent law draft that fills `Seadus`, a staged `.msg` is still never unpacked and its subject is capped one point below HIGH for the same reason, an EU act named by nothing but an annex beside a domestic instrument is background and is not offered at all, and the existing LOW/MEDIUM/HIGH contract, the conflict-fills-nothing rule and every manual-override protection are reused unchanged; no migration, no vocabulary change, no backfill and nothing stored
- 0081 — `Kodulehe ülevaade` is a dedicated Matter activity, and `koda.ee` is its boundary: a lawyer finishing a round of work routinely decides the membership should be told about it on the Chamber's website, and the file could hold neither half of that — the intention lived in somebody's head until the page appeared and the published address lived in a browser history, so «kas sellest kirjutati kodulehele, ja kus see on» had no answer on the Teema; a `Märge` cannot be listed or published, a `Document` is bytes in the evidence store, `Tulemuse tõend` and a `Submission` are about what Koda *sent* and folding a membership write-up into that vocabulary would corrupt every submission statistic, a `Töövõit` claims something was won, and a `NextAction` or a deadline would make every Matter carrying a plan read as late from the moment it was typed in — so it is its own Matter child record with three states (`Plaanis` carries no address and no date because neither exists yet, `Avaldatud` requires both and the database says so, `Tühistatud` is a dropped plan kept rather than deleted), no `Avaldatud → Tühistatud` in v1 because the page is on koda.ee and a record denying it would have the file disagreeing with the website, and a publication date that is always the person's and never the server's (docs/adr/0078 §2 applied before the mistake rather than after); the address boundary is `https` and a **parsed host** equal to `koda.ee` or ending in `.koda.ee`, which is what separates it from `koda.ee.example.com`, from `https://koda.ee@example.com/` where the name is in the userinfo a browser ignores, and from `notkoda.ee`, with userinfo refused outright and an over-long address refused rather than truncated (red-team findings F-1, F-2); it reads in exactly two places — a strip of what the file still owes, and the chronology's two completed milestones, projected from the record and rendered as `Ava kodulehel` rather than a raw URL — and in v1 touches no work list, no deadline, no search or archive projection, no document listing and no Submission or work-victory metric; closing a Matter cancels every plan it still owed in the closure's own transaction, each with its own audit event and never blocking the closure, while a published address stays correctable on a closed file under the project's expected-revision protection, because closure means no new content and has never meant that a fact recorded wrongly must stay wrong (docs/adr/0075 §12); four audit event types, one new table and one `choices` alteration, and no data migration or backfill of any kind.
- 0082 — `Kaasamise kuupäev` is recorded at the precision it is known to (narrowing ADR 0079 §11 for that one date, and completing ADR 0078 §2's deferred question): everything else on §11's list is a day this office recorded or owes and is therefore known exactly, while an engagement date is a statement about when something happened out in the world and is routinely written down afterwards from a mail folder that gives a month or a register row that gives a year — so `+ Kaasamine` and `Muuda` both take the four chips of ADR 0079 §1 through the existing `period_composer.html` and the existing `bounds_for`, no `Poolaasta` and no `Tuletatud tekstist`, and the stored value is the period's anchor which no surface may print as a day — not the chronology, not the correction form's date box, not *Viimane tegevus*, not *Viimati muudetud*, not the audit payload; `Tagasisidet ootame kuni` is named explicitly and **stays exact and inert**, because it is a day somebody stated to other people and «palun vastake oktoobris» is not a deadline anybody was given; one additive column `occurred_on_precision` with a vocabulary `CHECK` and deliberately **no** `period_end`, because an engagement already happened and is never late, overdue, a deadline or a work item; an unknown date has no precision and normalises back to `EXACT`, so widening the control cannot turn «I do not know» into an approximation; no backfill and no inferred historical precision, every existing row having been written through a box that asked for a day
- 0083 — a feedback deadline is a dated point, and a kodulehe ülevaade can arrive already published (narrowing ADR 0074 §12.1 for one source and ADR 0081 §1-§2 for two optional fields): `MatterEngagement.feedback_deadline` was readable only by scrolling to the engagement's own chronology row, so it now draws a `Tagasiside tähtaeg` column on the process strip — one per round that carries a reply-by date, «Keda kaasati» as the column's title, exact day, scoped through the child's own `visible_to` before the strip is derived — while the engagement itself stays retired from the strip and a round with no reply-by date still draws nothing; it creates no NextAction, no work item, no badge, no lateness and reaches no work list, statistic, search row or archive projection, because drawing a date is a reading of the record and not a new obligation; and `+ Kodulehe ülevaade`, which presented as a fieldless panel with a vague `Salvesta` and read as a broken text box, now offers `Kodulehe link` and `Avaldamise kuupäev` as an optional pair under their own legend — neither filled is the plan the button now names outright (`Lisa planeeritud ülevaade`), both filled records `PLANNED → PUBLISHED` in one act through the existing `publish_website_overview` so the koda.ee boundary and the audit events stay in one place, one filled is refused naming the other with what was typed preserved, the date carries no `initial` because a pre-filled one would make the empty plan unreachable, and a planned row's disclosure now says `Lisa link ja avaldamiskuupäev` rather than naming the transition — **amended 2026-09-16**: the primary action is `Lisa ülevaade` rather than `Lisa planeeritud ülevaade`, because a label naming the plan is untrue of the same form's other outcome — a submission carrying both a `Kodulehe link` and an `Avaldamise kuupäev` files a `PUBLISHED` row — so the button names the record it creates and the plan is named where it is chosen, in the optional pair's own legend; the two-path form, the koda.ee boundary, the refusal, the audit events, the lifecycle, the closed-Matter rules, the absent `initial` on the date, the planned row's `Lisa link ja avaldamiskuupäev` disclosure and the whole of the feedback-deadline half are unchanged
- 0084 — `Väline seisukoht` is a dedicated Matter activity, and its source is the boundary: a lawyer constantly learns what other organisations think about a file — a ministry's press release, an association's position paper, a member's written answer — and none of it could be filed anywhere, so the address lived in a browser history and the PDF in a mail folder; six existing records could have absorbed it and each would have lost something (a `Märge` is prose nothing can question, a `Submission` and `Tulemuse tõend` claim Koda *sent* it and would corrupt every submission statistic, a `Töövõit` claims something was won where a position may be against us, a `NextAction` or a deadline would make every Matter carrying one read as owing work, and a `Document` alone cannot say whose position it is or when it was stated — which is why `DocumentRole.EXTERNAL_POSITION` had existed since the foundational schema with nothing writing it), and it is not a `Kaasamine` either because an engagement records that Koda **asked** and this records that somebody else **said**; so `MatterExternalPosition` requires an organisation from the one shared catalogue and a source — a public `http(s)` address checked by parsed host, an ordinary attached `Document` through the existing `DocumentLink` architecture and the `EXTERNAL_POSITION` role, or both — with an optional `Seisukoha kuupäev` at all four precisions or none, an optional short `Selgitus`, and an optional relation to the `MatterEngagement` it answered, which must stay optional because the commonest position is unsolicited; the source rule cannot be a `CHECK` because one half is a column and the other a row in another table, so it is decided in the service before the insert and again on every correction against the link table; it reads on the chronology alone as «Väline seisukoht: [organisatsioon]» with the link labelled by its host or `Ava seisukoht` and never as raw text, and it reaches no `NextAction`, deadline, work list, statistic, search projection or archive row in v1; four audit events cover recorded, corrected, source changed and document linked; a closed Matter refuses a new position **and** a correction, following `Kaasamine` rather than `Kodulehe ülevaade`'s narrow exception, because every field here is substantive and reopening is the way out; three additive migrations, no `RunPython`, no `RunSQL` and nothing to backfill from — **amended 2026-09-16**: the source minimum becomes one of **three** rather than one of two, because the commonest feedback a department receives — two sentences by e-mail from a member association, something an official said on the telephone — has neither a file nor a published page, and refusing it bought a fabricated source rather than a better-sourced file; the optional `Selgitus` is accordingly renamed `Seisukoht` and holds what the organisation actually said, which makes text-only, link-only, file-only and every combination valid while a record holding none of the three is still refused in Estonian naming all three; and `+ Väline seisukoht`'s date box now opens on today at `Täpne päev`, visibly and clearably, the way `+ Kaasamine`'s does, while the correction form keeps no default so a recorded position never reopens showing today; the rule stays in `_external_position_source` because a `CHECK` cannot count `documents_documentlink`, and the whole amendment is one `AlterField` over a `verbose_name` with no data migration, no backfill and no rebuild of any kind
- 0085 — an `Ülevaade / uudis` is one neutral publication activity, and any public web address will do (superseding ADR 0081 §3 and ADR 0081's name for the record, and narrowing ADR 0083 §2 on the date default): ADR 0081 wrote one true observation — the membership should be told about this file on koda.ee — into both the record's *name* and its *validator*, and the write-up turned out to appear in trade papers, member newsletters, partners' sites and ministries' news feeds as well, so a lawyer holding the address of a piece about their own Matter had nowhere to put it, which is the condition ADR 0081 existed to end; it is therefore one neutral activity called `Ülevaade / uudis` with **deliberately no type selector** — a `liik` would be asked at planning time, when there is frequently no decision yet about where the piece will run, would be blank or guessed on most rows, and would be a classification nothing consumes and therefore nothing corrects, while the **link already answers the question from a fact** (the reasoning ADR 0054 gives for retiring `NextAction.kind`); the host allow-list is gone because its premise is gone and a list maintained under pressure to widen it says nothing after a year — `Väline seisukoht` reached the same conclusion two days earlier (ADR 0084 §3) — so any syntactically valid public `http`/`https` address is accepted while every safety rule is kept: scheme allow-list, a **parsed host** and never a substring, userinfo refused outright, over-long refused rather than truncated, shared with the engagement and position normalisers through `_normalize_public_link` rather than copied; **nothing fetches, crawls, resolves or previews the address** and nothing claims it is still live, so the widened rule opens no request this application did not already decline to make, and the row still renders `Ava ülevaade või uudis` in a new tab with `rel="noopener noreferrer"` rather than a raw URL; the publication date keeps **no form `initial`** — that is what makes «neither box filled» reachable and the plan recordable — and instead fills itself *visibly* with the **server's** today the moment somebody starts typing an address, once, never over a value already there and never again once the person has touched the date box, so a date cleared on purpose stays cleared and a form nobody touched still submits two empty boxes; the three states and their transitions, both-or-neither in the database, zero-one-or-many per Matter, no deletion, the four audit events and their absence from the chronology's event types, the closed-Matter rules in both directions, the expected-revision protection and every one of ADR 0081 §4's eleven absences are restated and unchanged; the model, the `WEBSITE_OVERVIEW_*` stored event values, the routes, the DOM ids and the CSS classes keep their names because none of them is a surface anybody reads; two **state-only** migrations — one `AlterModelOptions` and one `AlterField` over a `choices` list — and no `RunPython`, no `RunSQL`, no backfill, no data migration, no search rebuild and no archive rebuild, the address boundary never having lived in the database at all
- 0086 — a `Kaasamine` waits for answers, and somebody finishes it: a consultation round disappeared the moment it was filed — the reply-by date ADR 0078 §3 added was deliberately inert, so a file asking members to answer by the 22nd showed on no work surface, said «Järgmine samm on määramata» on its own page, and had nowhere to put what came back; so an open `feedback_deadline` on an open FULL Matter is now a fourth `WorkItem` source owned by the Matter's owner (ownerless ones reach the department's *vastutajata* surfaces and nobody's personal desk), waiting before its day and due on and after it — where what is late is this office's unread post and never the people who were asked — while every other property ADR 0078 §3 and ADR 0083 §1 gave the column stands: no `NextAction`, not `Matter.response_deadline`, no `MatterImportantDate`, deliberately outside `real_deadlines` so no *Tähtajad* panel or register deadline group counts a consultation as a promise, and no work-victory, search or archive projection; it never overwrites an open `Järgmiseks` — both render, truthfully, side by side; `Lõpeta kaasamine` on the round's own chronology row ends it, with optional written feedback (`feedback_received`), optional files through the existing evidence path, a dedicated `ENGAGEMENT_FEEDBACK_CLOSED` event carrying actor, timestamp, deadline and reason but never the words, refusals for a round nobody is waiting on and for a wait already finished, and no partial write on a stale revision; `close_matter` ends every open wait in the transaction that shuts the file, audibly and without ever being blocked by one, and a reopen does not revive them; the creation form is simplified to `Keda kaasati` + two visibly defaulted, clearable dates (today, and today + 7 with `1 nädal` / `2 nädalat` / `1 kuu` calendar-month spans resolved server-side) + the feedback box, with `Liik` and the `Täpsus` selector removed from both `Kaasamine` surfaces — narrowing ADR 0082 §1 and the Teema redesign §14 — while every stored kind and every stored period keeps its meaning, the chronology prints a channel only when it is not `OTHER`, and a record dated to a period opens with an empty day box that means «leave it alone», with `Kustuta salvestatud kuupäev` the one deliberate way to clear it; two additive migrations, no `RunPython`, no `RunSQL`, no backfill and no reindex
- 0087 — the similar-Matter finder extends one engine, and reaches `Uus teema` (extending ADR 0062 and reversing none of it): the deterministic, explainable, permission-filtered recommendation ADR 0062 built already proposed related Matters, said why each appeared, showed five, never showed a score and wrote nothing, so this round added the three things it lacked rather than a second matcher with a second set of weights — a lawyer must not learn that the cards under the create form and the cards in the Matter rail answer one question by different rules, and a candidate that qualifies while a file is being typed still qualifies the moment it is saved; **`Õigusakt` became a signal**, a shared `LegalInstrumentType` reading «Sama õigusakti liik: Määrus» and weighted like a policy area because seventeen values cover the register and `Seadus` covers much of it by itself — alone it is 1.5 against a threshold of 3.5 and beside a ministry 2.5, both below the line by construction and both tested, `Muu` is not a match because it is the vocabulary's escape hatch and two files that both failed to fit the list have agreed about nothing, the free text behind it is not compared either, and the label is deliberately distinct from the named act read out of a title so a card carrying both tells a reader two true things rather than one thing twice; **a card now says what Koda did there** — a recorded opinion, a sent Submission, a confirmed `Töövõit`, as existence and never as content, with no count, no date and above all no generated précis of an earlier position, each computed under the reader's *own* child visibility because a visible Matter may carry a restricted opinion and a badge asserting one exists is the disclosure ADR 0038 forbids, in three queries for five cards or fifteen and computed after the cut so they can decide no rank; and **`Uus teema` asks the same engine** over a GET that creates no draft Matter, writes no relation, dismissal or audit event, proposes nothing acceptable because there is no Matter yet to attach a relation to, opens a candidate in a new tab because the form behind it is unsaved, replaces one region so nothing typed can be swapped out from under the person typing it, resolves catalogue keys against the reference vocabularies rather than trusting the request, recomputes only on the fields that decide the answer, and answers with silence rather than an empty box below a subject word or two structured facts; hiding when empty is that surface's rule **only**, because ADR 0062 §7 rejected computing suggestions on the ordinary Matter render and the reason has not changed, so «Võimalikud seosed» keeps its deferred disclosure and its honest-emptiness message; no sector signal because there is no sector model and inventing one to satisfy a label would be manufacturing a taxonomy from a wish, no structured Koda-position signal because `position_summary` is prose and matching on a stance the record does not hold would be inferring one, no generative AI, embeddings, external service, click history or raw document-content similarity, and still nothing linked automatically; no model, no migration and no new index, with `INDEX_VERSION` and `ARCHIVE_INDEX_VERSION` untouched so it deploys with no rebuild
- 0088 — `Uus teema` is manual-first, after the lawyers' first structured feedback on the demo: the document reader's suggestion panel, its `data-prefill-*` markers, its reading states and its `suggestion_state` field are **withdrawn from the creation form behind one setting** (`MATTER_INTAKE_SUGGESTIONS_ENABLED`, read by `create_form_suggestions_offered()`) rather than deleted — the rules, vocabularies, reader and corpus tests are untouched and `tests/test_intake_staging.py` exercises them with the switch on, staging and promotion of *files* are unaffected because uploading a document and being offered a reading of it are two features that merely met on one screen, the gate sits at `_intake_context` above all three surfaces so nothing mutates the form invisibly, the browser's poll stops by itself because it polls only while the panel reports `reading`, `Muuda teemat` keeps its reading because that page is opened in order to look at it, and ADR 0087's `Sarnased teemad` is preserved in full since it proposes Matters and never form values; **Saatja opens as an empty search box** via one `quiet` flag on `organisation_picker.html` that marks the shortlist chips the way the long tail already was — present as real controls, `hidden` until searched out or chosen, with ADR 0073's resolver contract, the alias reuse, the ambiguity refusal and ADR 0069's Adressaat default all unchanged, an answer never hidden, and the `<noscript>` fallback carrying the shortlist as well as the tail; and **Valdkond becomes a `<details>` whose summary carries the answer**, `data-stay-closed` so a refused save comes back shut rather than unfolding to prove what the summary states, server-rendered open only for a refusal or a ticked `Muu`, same checkboxes and same many-to-many per ADR 0025 — with vocabulary version 4.0 (`taxonomy/0007`) withdrawing `Koalitsioonilepped` (a *source document*, which is `Õigusakt`'s question per ADR 0070) and `ELi õiguse ülevõtmine` (Menetlusliik spelled twice — `Track.NATIONAL_TRANSPOSITION` has carried those four words since `matters/0001`) by `is_active=False` alone, never remapped and never deleted, with nothing inferring the track from the withdrawn area, no reindex because no relation moved, and the register filter still resolving a withdrawn key so a bookmarked filter survives
- 0089 — `Menetluse link` is a reference, and an `Ülevaade / uudis` publication date is optional (implementing the master specification's §11.2 `ExternalReference` narrowly; amending ADR 0081 §2 and §6's first implication, and **superseding ADR 0085 §3** entirely): two decisions from one round of lawyer testing, both about the application asking somebody to state a fact they do not have and getting a plausible answer instead of a true one. **Where the proceeding lives** became a record — five lawyer-chosen kinds (`EIS`, `Ministeeriumi dokumendiregister`, `ELi menetlus`, `Riigikogu`, `Muu menetluslink`), an address, an optional short name — with the kind **stated rather than proved from the hostname**, because a ministry runs several registers and a host rule would silently reclassify every stored row the day one moved domain, which is ADR 0084 §3's and ADR 0085 §2's conclusion about allow-lists in the shape a vocabulary takes; it is a **reference and not an ingestion** — nothing is fetched, crawled, polled, resolved, previewed or watched, no `Document` is created from it, no date or metadata is read off the other end and no task system is introduced, which is the master specification's own §20.2 ordering and §11.2's warning against presenting a static link as synchronized — zero, one or many per Matter with **one address per Matter** so a double-click cannot duplicate a pointer, no deletion (a wrong kind, name or address is corrected, and a correction is allowed on a closed Matter where an addition is not), a rail card that renders nothing at all when there are none rather than five empty placeholders, a tenth launcher chip, one prefixed and **folded** block on `Uus teema` saved atomically with the Teema — folded because ADR 0088 came out of the same feedback round and is about this very page reading as a survey when too much on it is expanded at once, so it takes `Valdkond`'s own disclosure shape and says what it holds on its summary rather than by unfolding — and deliberately **no chronology row, no search row, no work item and no KPI**; and the **publication date became optional** — lawyer testing measured ADR 0081's refusal and ADR 0085 §3's paste-triggered default producing the same wrong answer from opposite directions, people typing today to get past a refusal and accepting today because it was already in the box, so a page is now published because it is up at an address rather than because somebody knows which day it went up, `NULL` means *unknown* and nothing substitutes the clock, `created_at`, `published_at` or a sentinel for it, the script island and the publish form's `initial` are both withdrawn, a date with no address is still refused, clearing a date is an ordinary correction that does not spring back, the chronology prints «Kuupäev teadmata» and places the row on the day it was recorded without describing it that way, and **existing stored dates are neither cleared nor guessed at** because no provenance distinguishes an accepted default from a typed answer; three migrations, all state-only or schema-only, no backfill, no reindex and no release-note or deployment change
- 0090 — one question per classification on `Uus teema`, after the lawyers' second structured feedback round (upholding ADR 0070 on `Õigusakt` not being `Menetlusliik` and ADR 0032 on stage not being disposition, superseding ADR 0069 on the create surface only, and building on ADR 0088): the page asked a lawyer to classify one file four ways and two of the four were the same answer twice, so it now asks three — *mis seisus asi on*, *mis tüüpi objektiga on tegemist*, *kes selle meile saatis* — in the order Saatja · Valdkond · Hetkeseis · Õigusakt; **`Hetkeseis` version 2.0 rewords three labels and does nothing else** (`Ootan jõustumist` → `Jõustumise ootel`, `Eesti seisukoht` → `Eesti seisukoht koostamisel`, `Ootan ELi õiguse ülevõtmist` → `ELi õiguse ülevõtmise ootel`, with keys, rows, relations, help texts, sort orders, filters and reporting all untouched because a stage is addressed by its key everywhere it is stored) and clears the `is_provisional` flag `workflow/0004` set pending exactly this review — nothing retired, so PR #231's preservation contract is asserted against a stage retired inside the test rather than used, and nothing **added**: the feedback asked for `Rohkem ei tegele` as a Hetkeseis and it is not one, because *Koda has stopped working on this* is a statement about this office rather than about the external process and the product has modelled it as `Disposition.MONITORING_STOPPED` since ADR 0032 — offered already on `Lõpeta teema` as «Koda ei tegele edasi» and in the composer as «Loobuti», and the reading `workflow/0004` has given the workbook's own `rohkem pole tegevusi plaanis` since 2011, so a stage with neighbouring words would have put two questions in one column and made the historical reading disagree with the current vocabulary about the same words; `Jõustunud` likewise remains a stage that ends nothing; **`Õigusakt` version 2.0** replaces seventeen register-derived spellings with ten reviewed types in two named groups — `VTK`, `Seadus`, `Määrus`, `Koja ettepanek või pöördumine`, `Strateegia, arengukava või tegevuskava`, `Muu siseriiklik`, `ELi konsultatsioon`, `ELi direktiiv`, `ELi määrus`, `Muu ELi dokument` — reusing `direktiiv` and `el-maarus` under clearer labels because each is *objectively identical* and a new row beside either would split one classification in two, while refusing to reuse `konsultatsioon` (whose own description covers a domestic consultation), `ettepanek` (which covers a Commission proposal) or the three strategy rows, and withdrawing twelve by `is_active=False` alone — rows, keys, descriptions, relations, statistics, register filters and chips all kept, nothing remapped, nothing deleted, and the five new rows carrying **no aliases** so `canonical_legal_instrument_keys` still reads a 2014 «strateegia» cell as `strateegia`; `Muu` becomes a *set* of three rows (`OTHER_LEGAL_INSTRUMENT_KEYS`) whose shipped behaviour is unchanged, folded onto the first such row only in the rail, excluded from the similar-Matter signal in all three because a shared «none of these fitted» is a shared absence, and never inferable by any rule; **`Menetlusliik` leaves the create form and is derived from nothing** — the reviewed labels name *siseriiklik* or *ELi*, so the distinction the lawyers kept is **readable** from the type a Matter already carries (`DOMESTIC_LEGAL_INSTRUMENT_KEYS` / `EU_LEGAL_INSTRUMENT_KEYS`), but that is a reading and never a source: `Matter.track` says what kind of *procedure* a file is on, it has seven values rather than two, no instrument type entails one, and a `Seadus` transposing a directive is a domestic instrument on a `NATIONAL_TRANSPOSITION` track — so writing `DOMESTIC` from `seadus` would reduce a seven-value classification to a domestic/EU boolean and be wrong about exactly the files the distinction exists for; the column is answered where its meaning is known, by a person, on `Muuda teemat` and in the Teema rail, and **edits write nothing either**, so a stored `track` is never rewritten by an edit about something else — the column stays **canonical** while an empty value means *not known yet* rather than a default or a value to be inferred later, and the siseriiklik/ELi grouping is a **projection** computed where it is shown, never persisted back into it; **`Adressaat` leaves the create form** with the whole ADR 0069 default, its hidden `addressee_is_manual` marker and its browser half, while the column, the relation, `resolve_addressee`, the `Kellele` rail row, `Muuda teemat`, the submission workflow and every stored addressee stand — Saatja and Adressaat are different facts and are never merged; and **`Kellelt` becomes `Saatja`** on the rail and the edit form, renamed rather than removed because it is the only place the Teema page states who sent the file; `Matter.track` keeps its column, values, filter, metric, audit event and `applicable_tracks`; two `RunPython` migrations (`workflow/0007`, `taxonomy/0008`), one leaf per app, no schema change, no backfill, no row created in `workflow`, no reclassification, both failing closed on a row renamed since review, with `workflow/0007` reading no other app in either direction after CI proved why a reverse may not (`migrate <app> zero` rewinds the other app first); the process strip never reads `StageVocabulary` and is unaffected; and **no reindex** — the Matter projection carries neither a stage nor an instrument label, so `INDEX_VERSION` and `ARCHIVE_INDEX_VERSION` are untouched
- 0091 — one lawyer workflow, from `Teema` to the next `Koja arvamus` (numbered 0091 rather than 0088 because PR #230 merged its own 0088 first, Package B took 0089 and Package A claimed 0090 on the same day; narrowing ADR 0086 §2 on where a feedback wait comes from, extending ADR 0084, ADR 0061 and ADR 0052, and reversing none of them): the first lawyer test reported five things and they were one thing — the product held every fact the work produces and did not hold the work; so `Uus teema` gains a `Koostan arvamuse` date that atomically creates the ordinary `NextAction` from a day the person supplied and **never** from one the application picked, refusing a save that answers both first-step boxes and returning an existing equivalent step on a retry rather than writing a second; `+ Kaasamine` stops asking `Tagasisidet ootame kuni` **at all** — not an empty box but no box, because recording that Koda asked somebody something is a completed act — and the managed wait ADR 0086 built is opened by one explicit act on the round's own chronology row, `Ootan tagasisidet`, with the column, the work item, `Lõpeta kaasamine`, the closure columns, the correction form's box and every round already waiting untouched and no migration; `Väline seisukoht` splits into `+ Meile saadetud tagasiside` and `+ Teiste arvamus` over **one** record through an explicit `provenance` column that is never inferred from a linked round, a named organisation, a supplied URL or an uploaded file — with `LEGACY` for the corpus that predates the question, no backfill, and those rows still reading under their original heading; received feedback may name a `source_label` instead of an organisation, so a survey of 234 companies needs no invented «234 ettevõtet»; a separate `Juristi märkus` keeps this office's reading of a position out of the position, stored, rendered, audited and indexed apart from it; `+ Koja arvamus` records the Chamber's own opinion on the Teema page through the same `register_sent_opinion` service `Dokumendid` posts to — no `KodaOpinion` model, several per Matter, recipients never defaulted from the `Saatja`, per-opinion keywords deliberately not introduced and the publication association deferred with no publication model added; `+ Menetluse areng` is the round's **one new model** — `MatterProceduralDevelopment`, because Package D established that an incoming development cannot be projected truthfully from an `Entry`: its date may honestly be unknown, its title must be readable without parsing prose, and the lawyer's note must not become the ministry's own sentence — carrying an optional `Hetkeseis` and next step in one transaction, so a file does not end at «Arvamus saadetud», and deliberately carrying **no** relation to Package B's `MatterProceduralLink`, which is a reference and never evidence that anything happened; and one sentence on `PRAEGUNE TEGEVUS` says the procedure may continue without inventing any work; four additive migrations, no backfill, no reindex, and Package D still owns the unified substantive history. **Amended 2026-09-26** (ENG-043): the reply-by-before-round rule is enforced by `add_engagement`, `update_engagement` and `open_engagement_feedback_wait` through one function, `feedback_deadline_precedes_engagement`, which compares the exact deadline with the first day of the engagement date's whole period rather than with the stored value; the forms ask it early
- 0092 — substantive Matter history, and an evidence-safe process timeline (extending ADR 0074 §14–§16, ADR 0091 §5 and ADR 0079; preserving ADR 0032 and ADR 0089 §2 exactly; reversing nothing and retiring no record): the second lawyer round asked the Matter page *mis selle teemaga päriselt juhtus?* and the page answered by talking about itself — one `+ Menetluse areng` save drew **three** rows so the reason for two of them sat two rows away from the fact, a confirmed `Töövõit` was dated to the afternoon somebody pressed `Kinnita` rather than to its own business period, a sent `Koja arvamus` was read from the send event whose `occurred_at` for a reconstructed historical opinion is the day of the *import*, an open `Järgmiseks` read at the top of the page and again halfway down as «määras järgmise sammu», and the strip above the list was labelled `Teema käik` while drawing a deadline three weeks out; **this is a projection package** — no model, no table, no migration, no `INDEX_VERSION` bump, no reindex, no archive rebuild and no reporting metric, the single write in the diff being two additive keys in an existing JSON payload — and `app/matters/timeline.py` was **evolved rather than replaced**, because it already projected canonical records, already grouped by `ChangeEvent.operation_id`, already kept field-level noise off the list and already filtered every source through `visible_to` before sorting, so a second engine would have been two answers to «what happened» with no way to tell which was meant and no `MatterHistoryItem` type or `Event` model was added; `Submission` and `MatterWorkVictory` become canonical sources so that **no business date on this page comes out of an audit row** — a sent opinion reads `sent_at`, its `ADDRESSEE` rows, its `Liik` where it is not the default and its `final_version` bytes, several per Matter each with its own row and nothing collapsed into a «final opinion»; a work victory reads `period_date` + `display_period` while `confirmed_at` decides only *whether* the row exists; **unknown means unknown** — «Kuupäev teadmata» is printed and `created_at`, `updated_at`, an audit or upload timestamp, the operation's moment and today are all refused as substitutes, with internal timestamps placing a row deterministically and never describing it, and an approximate date sitting on its period's anchor while printing the period; **grouping is `operation_id` and nothing else** — no time window, no shared author, no similar title, no matching filename and no prose parsing — so the development, the stage change and the next step of one save read as one act with `Hetkeseis →` and `Järgmiseks →` under it, folded **only** onto a record already on this reader's page so a restricted sibling leaves a Matter-level stage change standing exactly as one made from the header; the open `Järgmiseks` stops drawing a standalone history row while a step set inside a note or a development keeps its strip there and a superseded step comes back as history; `Ajajoon` is renamed **`Teema käik`** with `#ajajoon` and `?ajajoon=` untouched because they are what a shared link addresses, and the dated strip keeps every column, source and state ADR 0074 §12 and ADR 0083 decided with only its `aria-label` moving to `Menetluse tähtajad`; a read-only **`Kõik muudatused`** page per Matter keeps the writes the primary history leaves out auditable — buildable only because `scope_change_events` already existed, showing when, who, which kind and the write's own summary, and showing no payload, no primary key, no `operation_id` and no `SecurityAuditEvent`; and **`Menetluse kulg` V1** is a deterministic read-only rail over two generic templates (`Algus · Kooskõlastus · Valitsus · Riigikogu · Jõustumine`, and `Algus / konsultatsioon · Eesti seisukoht · EL menetlus · Vastu võetud · Ülevõtmine / jõustumine`) with **four** states in words — `Praegu`, `Kirjas`, `Teadmata`, `Võimalik` — because a current stage proves the current stage and not every prior milestone, so a Matter first filed at `Riigikogus` reads `Teadmata` for the three nodes before it and the word «tehtud» is not in the component's vocabulary; a node is `Kirjas` only from `Matter.stage` or the explicit `MATTER_STAGE_CHANGED` history and never from a title, a filename, an organisation, a `MatterProceduralLink`, a URL host, the date or a node's position; the rail carries **no dates**, `Muu` and a European stage on a domestic file read beside it rather than being forced onto a node, `Rohkem ei tegele` stays `Disposition.MONITORING_STOPPED` beside the rail and `Jõustunud` still closes nothing; the template is chosen from `Matter.track` where its semantics safely can and otherwise from Package A's `DOMESTIC_LEGAL_INSTRUMENT_KEYS` / `EU_LEGAL_INSTRUMENT_KEYS` as a **projection that never writes a track** — `NATIONAL_TRANSPOSITION` is never inferred from `Seadus`, a mixed-instrument file draws no rail at all and neither does one with nothing to place — with no VTK, government-regulation, minister-regulation, Koda-initiative or transposition-specific workflow, no `Määrus` subtype invented for decoration, no configurable editor, no node inference and no register fetching; `change_stage` additionally records `from_key`/`to_key` so a stage is addressable by its stable key from the history, additive with nothing backfilled and older rows resolved live through the reviewed vocabulary
- 0093 — a `Koja arvamus` carries its own `Märksõnad` and its own `Ülevaade / uudis` links (resolving both questions ADR 0091 §6.5 and §8 left open, and resolving them the other way; the keyword vocabulary stays exactly one — governed `taxonomy.Tag` — and what is new is a second *assignment* of it, `submissions.SubmissionTagAssignment`, because a Matter's tags and an opinion's tags are two facts about two records and `matters.TagAssignment` is neither reused nor widened; no tag is inherited from the Matter, derived from a title, from the sent file's contents, from recipients, from the channel or from the `Liik`, and nothing is backfilled, so every Submission in the register starts at zero keywords; a new assignment resolves through `Tag.canonical()` and is refused if it is not active afterwards, while a tag already assigned stays selectable and ticked after it is deprecated or merged — the bound-value union `MatterEditForm` uses to keep a departed colleague a valid owner; `Submission` ↔ `MatterWebsiteOverview` becomes an explicit optional many-to-many through `submissions.SubmissionWebsiteOverviewLink`, copying no URL, title, status or date, with both endpoints required to belong to the same Matter — enforced at the service boundary and restated in `clean()`, never as a trigger and never as a denormalised `matter_id` — and a foreign overview refuses the whole save atomically; no link is inferred from a matching URL, title, date, chronological proximity, shared document, shared tag or status, and none is backfilled; `Plaanis`, `Avaldatud` and `Tühistatud` all participate because no new status restriction is invented, and linking publishes, cancels and addresses nothing; four new audit events, one per fact that actually moved, absent from `TIMELINE_EVENT_TYPES` exactly as `TAG_ASSIGNED` is, carrying identities and names but never `notes`, the lawyer's assessment or the sent file's contents, and a no-op writes none of them; unlinking removes the assignment row alone and leaves the Submission's status, `sent_at`, recipients and immutable evidence and the overview's status, URL and `published_on` untouched; both edits are `@business_write_required` with no open-Matter lock, the contract `set_tags` and `withdraw` already keep; visibility is asked of each endpoint independently so a restricted overview is never offered, counted or hinted at, a guessed identifier reaches nothing, the reciprocal list scopes `Submission.objects.visible_to`, and an actor can only unlink what they can see; and search is not redesigned — no child row in `child_indexing` carries taxonomy, tags reach the corpus once through the Matter's own projection, the `MatterExternalPosition` and `MatterProceduralDevelopment` precedents hold, and with no backfill a bumped `INDEX_VERSION` would rebuild the whole corpus to index nothing, so neither it nor `ARCHIVE_INDEX_VERSION` moves)
- 0094 — `Uus teema` answers with menus, and asks for one date (four corrections from one round of lawyer feedback, none of them a schema change: `Valdkonnad` and `Hetkeseis` become a shared `chipmenu` — a `<details>` whose panel is absolutely positioned, so the field below it does not move when the menu opens, which is what the browser suite measures rather than a class name — keeping the element for the tab stop, Enter/Space, the native expanded state and a control that still works with scripting off, and adding only Escape, click-outside and, for single-select, closing once answered; `Valdkonnad` stays a checkbox group over the same many-to-many with the same twenty-one governed areas and the same free-text `Muu`, ticking one does not close the menu, and the trigger carries a **count** because a one-line pill cannot hold three Estonian policy areas, while `Muu`'s box and both refusals sit outside the panel so a shut menu can never hide something that has to be read — which is why `policy_area_disclosure_open` is gone rather than renamed; `Hetkeseis` names its answer instead of counting it, «Määramata» included because it is a real chip the form arrives with selected, and its panel is deliberately uncapped because a scroll container would clip the `.stagehelp` bubbles — same eleven stages, same explanations, `Rohkem ei tegele` still a disposition, nothing inferred and no `Matter.track` write; `Menetluse link` is open on arrival with no summary, no `chosen_summary` and no `disclosure_open`, asks for an address and an optional `Nimetus` (a label change only — still `required=False`), drops its five source chips and its explanatory paragraph, and files every row under the enum's existing `ProceduralLinkKind.OTHER` through a **constant** rather than a hidden input, so a forged `menetlus-kind=EIS` binds to nothing — no new value, no migration, nothing inferred from the URL and no historical row rewritten, with the other four still offered on the Teema page's own panel and the correction form; and `Arvamuse tähtaeg` becomes the one date the page asks, moved out of the file row to the last position above `Loo teema`, where entering it both records `Matter.response_deadline` and establishes the canonical `Koostan arvamuse` step once through an unchanged `establish_opinion_preparation_action` — a deliberate reversal of ADR 0091 §1.2 on that one point, because asking the same date twice under two names converted a real distinction nobody can act on at capture time into double entry, while `Matter.response_deadline` and `NextAction.target_date` stay separate columns with separate meanings and ADR 0078 §3 stands; `InitialOpinionActionForm`, its partial, the whole `Järgmiseks` block with its `Millal?` chips and `TWO_FIRST_STEPS_REFUSAL` are all retired from creation with no hidden inputs left behind, a blank box still invents no date and creates no step, and `workflow_one_open_action_per_matter` is untouched)
- 0095 — Simpler opinion and feedback capture on the Teema page (five simplifications to four `Lisa teemale` panels, two additive columns and nothing rewritten: `+ Koja arvamus` swaps its whole-catalogue chip row for the shared `quiet` organisation picker and opens it holding the Teema's `Saatja` — an *initial value* on an unbound form only, so a refused save does not resurrect a recipient somebody removed and `Matter.source_organisations` is neither written nor read back, reversing ADR 0091 §6.3 on both its arguments because the picker's `+` creates only through `resolve_organisation_name` inside the save's transaction and a default somebody can see is what ADR 0078 §2 allows; `Pealkiri` is replaced by `Kokkuvõte` in a new additive `Submission.summary`, not a renamed `title` (that column is the record's `NOT NULL` identity, printed in the outbound register) and not `notes` (bookkeeping, and already the search body), with `title` on this path the uploaded file's own name — where `add_matter_koda_opinion` has always taken it from when the box was blank — nothing read from the file's contents, nothing generated, no headline cut from the summary and no row backfilled, the summary rendering first in the chronology sub-line as `Seisukoht` does for an external position, and `INDEX_VERSION` bumped for exactly this reason because the descriptive text moved out of an indexed identity tier into a column the projection did not read, `ARCHIVE_INDEX_VERSION` untouched; `+ Teiste arvamus` and `+ Meile saadetud tagasiside` ask one exact date opening on today instead of the four precision chips, which stay on `Muuda` along with every stored `MONTH`/`QUARTER`/`YEAR` row, and lose `Juristi märkus` and `Seotud kaasamine` from creation — fields *deleted* rather than hidden, so a crafted POST reaches a form that never cleaned them, while stored notes and links keep rendering under their own labels and are still never concatenated into the position; `Allikas` comes off the received-feedback panel, which therefore requires an institution — the authorship rule satisfied as always rather than relaxed, `source_label` kept on the model with both `CHECK`s standing and `ExternalPositionEditForm` still offering the box per record — and `Liige` takes its place: explicitly ticked, derived from nothing (not the registry, the CRM, the name or a domain), never recomputed when membership changes, refused on any other provenance by the absent field *and* the service *and* a new `CHECK`, `False` on every existing row and no search facet; and `+ Ülevaade / uudis` becomes a day and a required address whose valid save is `PUBLISHED`, retiring the empty-form `Plaanis` — a save whose meaning depended on what nobody typed — while `PLANNED`, `CANCELLED`, `plan_website_overview`, `Avalda` and `Tühista` all stand, the date default narrowing ADR 0089 §8 only for the visible box and not for the withdrawn paste-triggered island, with every date default in the record a **form default** and never an inference: no historical row, import or unknown date gains one)
- 0096 — `Uus teema` is the master, and a Teema can be deleted (four corrections from the owner's live audit of the Teema screens; Packages 1–3 need no migration, Package 4 adds two nullable columns and one `choices` entry and touches no existing row): **`Uus teema` is the canonical design**, stated as a direction rather than a resemblance so that a future disagreement between the creation and correction screens has an answer — for every shared fact `Muuda teemat` takes the master's label, control, vocabulary, `Muu` rule and order, three new partials (`valdkonnad_field`, `hetkeseis_field`, `oigusakt_field`) carry the markup both pages include, both forms build their classification fields from one set of factories and mix in one `PolicyAreaChoicesMixin`, and the edit page may differ only by pre-filled values (retired vocabulary included, marked «kasutusest väljas»), by the three facts ADR 0090 §4/§5 deliberately keep off creation plus `Sildid` — now stated under «Ainult olemasoleva teema kohta» so the difference reads as a rule — and by the absence of `Märkmed`, which is `MatterPersonalNote` and belongs where the thought occurs, while the *business operation* stays two transactions with two form classes and two service paths; **`Valdkonnad` and `Hetkeseis` are visible chips again, reversing ADR 0094 §2** and retiring the `chipmenu` primitive outright (CSS, `bindChipMenus`, `bindChipSummaries`, `policy_area_chosen_count`, `stage_summary`) rather than leaving it unused, because a classification a lawyer can read without opening anything is what `Õigusakt` beneath them always was — an accordion open by default was offered by the brief and rejected as a collapse affordance for nothing — with nothing about the data moved and one consequence gained: `Muu valdkond` is asked the same way on both pages, superseding post-QA R2-07, whose objection was specific to a folded vocabulary since the chip renders ticked and its box renders open server-side whenever the Matter holds free text; **the ordinary Teema UI no longer exposes `Nähtavus`** — gone from `MatterEditForm`, `MatterFieldForm` and `IncomingIntakeForm`, from the edit page, the intake page and the Teema header's ⋯ menu, and out of `FIELD_SERVICES` so `matters:update_field` answers 404 for it, with no hidden input and no `clean_visibility` so a crafted `visibility=RESTRICTED` binds to nothing, while `Matter.visibility`, the enum, every stored restricted record, child inheritance, document restriction, the authorization layer, the restricted banner and `set_matter_visibility` itself are all untouched — a product removal from the business UI, not an architectural one, overruling DS-15 with the stated cost that material needing restriction is NORMAL in between; and **`Kustuta teema`**, a real purge with a documented residue, reached at `/teemad/<pk>/kustuta/` by GET to ask and POST to act (never a `confirm()`, never a GET side effect, CSRF as everywhere), authorised by the same `business_write_required` + `get_visible_matter` cohort that may edit — no narrower permission invented, because the product asked for the application's users to be able to delete and closure already sits with that cohort — walking reverse relations only from the one Matter (so `Organisation`, `Tag`, `PolicyArea` and the archive corpus are out of the set by construction, and a second `Matter` is never followed into) and removing every owned row in a topological order over the `PROTECT`/`RESTRICT` edges, leaving behind exactly the `Matter` row with `deleted_at` set plus its `ChangeEvent` history and any `ImportRowLedger` — because `audit.ChangeEvent.matter` is `PROTECT` onto a table with a `BEFORE UPDATE OR DELETE` trigger, so the row can be neither deleted nor detached and dropping that trigger would trade the audit guarantee for a tidier row count; the tombstone is invisible through the **default manager** rather than through several hundred remembered `.exclude()` calls (`Matter.all_objects` is the unfiltered one, `_base_manager` still resolves `ChangeEvent.matter`), and deletion **refuses in full** on a legal hold, on an append-only row hanging off a business child (a corrected `Entry`, whose `EntryRevision`s the database will not remove — refused rather than left behind, because a tombstone may keep audit proof and may not keep ordinary child records to look complete), on another Matter naming this one as successor, on any outside row pointing into the owned set under any deletion behaviour, and on a straddling row such as `EmailAttachmentLink` — with the plan rebuilt under a `FOR UPDATE NO KEY` lock inside the deleting transaction, evidence bytes left unreferenced for `prune_orphaned_evidence` rather than deleted before a commit that might roll back, and `INDEX_VERSION` deliberately **not** bumped because `SearchDocument` rows are owned and go with the Matter while the projection is built from `Matter.objects`

- 0097 — `Teema andmed`, `Lisa teemale`, `Teema toimingud` (nine decisions from the owner's reading of the screens 0096 produced; amends 0096 §1 and no migration anywhere): the Teema page has three regions and a reader can say what each is for — **what this Matter is**, **what happened to it**, **what you do to it** — and `EDIT_ONLY` is now the empty set, because 0096 §1's three carved-out fields are withdrawn rather than explained. `Sildid`, `Menetlusliik` and the Matter-level `Kellele` leave the ordinary Teema UI: fields **deleted rather than hidden**, so a crafted POST binds to nothing, `update_field` answers 404 for `track` and `addressee_organisation`, and `matter_edit` calls none of the three services — not even with a default, `set_organisations`' `_UNSET` being what keeps a register addressee through a save nobody asked to change it with. Every column, service, audit event and importer is untouched and `Koja arvamus` keeps its own recipients, which were always the fact people read `Kellele` as meaning. `Menetluse link` moves from the content launcher to `Teema andmed`, asked on both Teema forms from one partial and corrected under the revision the page was drawn from, with **no source classification and nothing inferred from the address** — no hostname rule, no fetch, no scraping. `Menetluse areng` retires as a *word* while `+ Märge` keeps writing its `MatterProceduralDevelopment`, because that record exists for a reason the round does not touch — a projection needs a title it did not have to parse — so the toolbar loses a concept and the database keeps a record; the new-entry panel drops `Täpsus` and `Juristi märkus` and the correction form still offers both, deciding per *record* where a statement about how well a date is known belongs. `Töövõit` asks for a day, on the owner's reasoning that a win is something Koda achieved and the organisation should be able to say when — historical `MONTH`/`QUARTER`/`YEAR` rows keep their periods and nothing is backfilled. `LISA TEEMALE` becomes four families with the distinctions asked second, grouped on the screen and distinct in the database, and `TEEMA TOIMINGUD` takes `Lõpeta teema` and `Kustuta teema` out of a row of capture controls where the most consequential thing looked exactly like the most routine one — the deletion protocol itself being 0096 §4's, unchanged, including its accepted refusal on a corrected `Entry`.
- 0098 — a file reads in phases, and the road ahead is conditional (succeeding ADR 0092 §13–§17 after lawyers used the two generic rails, and carrying two additive columns with no backfill): the lawyers' eight worked examples asked the Teema page for four answers, and the one with no answer at all was *grouped into understandable phases* — because **nothing in the domain said which phase an act belonged to**, `Matter.stage` being where the file stands now and `Õigusakt` what it is now, neither of which dates an opinion sent last spring. One eleven-phase vocabulary now serves both the rail's nodes and the history's headings, and a phase is deliberately **not** a `Hetkeseis`: `VTK` is not another spelling of `Idee`, `Kooskõlastusring` occurs twice on an ordinary VTK-then-bill file, and everything is keyed on the **occurrence** rather than the key. `MatterProceduralDevelopment.process_phase` is the one explicit association — proposed visibly in a select *beside the date box*, so somebody filing a step from 2019 is looking at the word while they type the year — and every other row is placed by a **business-dated interval** between two such steps, `[start, next start)`, with «unplaced» for anything else. An interval must be **unambiguous or it does not exist**: a current `Hetkeseis` naming a different phase proves the file left without saying when, so the tail reads under `Etapiga sidumata` until one `+ Märge` dates the step that moved it. Nothing is backfilled and nothing is inferred from a title, a filename, an organisation, a host, an upload time or a shared entry day — so on the day it ships every Matter renders flat exactly as before, `PhaseHistory.grouped` being false. Eight instrument-aware patterns replace the two generic rails and three of them are corrections rather than additions: an `EL määrus` gets **no** `Ülevõtmine` node, because a regulation applies directly and the old shared European ending was the rail inventing a legal obligation; `Jõustumine` and `Ülevõtmine` separate, because an EU act being in force is not transposition; and `Määrus` keeps one pattern with a **conditional** `Valitsuses` and no `Riigikogus`, since the reviewed value does not say whose regulation it is and the absence of a Government record is not evidence of a minister's. `Ees võib olla` names one to three phases in words, undated and conditional, and creates no `NextAction`, no deadline and no lateness — guidance may show unrecorded future phases, history may not. The dated strip moves into `Menetluse kulg` under `Kirjas olevad kuupäevad`, byte-for-byte unchanged, which reverses 0092's «replacing the dated strip» rejection narrowly: what changed is the *count* of diagrams, not the strip's decisions.
- 0099 — one rail, and the lawyer tailors it (the owner's cleanup round on the screens 0098 produced; one additive table and no backfill): the lower half of the Teema page carried a phase rail, an `Ees võib olla` list and a dated strip — three diagram-shaped things and seven labels explaining them — and the one thing a lawyer wanted to do with the roadmap, say *this file will never see a VTK*, was the one thing they could not. They are **one row** now, and it is the *strip's* markup that survived rather than the rail's: measured rather than chosen, because `test_process_strip.py` references `.tl-step` 21 times and `.tl-strip` 3, so keeping the strip preserved 300 lines of reviewed CSS, the `reached / today / ahead` grammar, the `--tl-reach` gradient and 51 browser tests. `Praegu`, `Kirjas`, `Teadmata`, `Võimalik`, `Ees võib olla`, `Kogu võimalik teekond` and `Kirjas olevad kuupäevad` are gone and **nothing became colour-only** — `aria-current`, a ringed dot, a visually-hidden «Tulevikus» and a dashed dot carry what the words did. `Ees võib olla` went projection and all, because it was a second rendering of what the rail beside it already drew. The merge also put docs/adr/0074 §12.1's defect straight back — `Kooskõlastusringil` under a node called `Kooskõlastusring` — found in a browser rather than by an assertion, so **`Hetkeseis` reads in the header and the phase on the rail**, which reverses 0092 §13's amendment on the ground that the `Praegu` it disambiguated is gone too. `MatterTimelineStep` makes the rail editable: hide a phase, date a phase, **store nothing for the ordinary answer** — and a phase the file has *recorded* is dated by that `Menetluse areng` and gets no box, because offering a second place to type the day is how two screens start disagreeing. `Kustuta` joins `Muuda teemat`, `Lõpeta teema` rejoins the chips in the same exclusive group, `TEEMA TOIMINGUD` is retired, and `Liige` moves into the organisation picker's footer. **Amended 2026-09-21** after the owner used it: a future dated point is now placed against *the phase it belongs to* — `Arvamuse tähtaeg` and `Tagasiside tähtaeg` belong to none and read beside the current phase, `Jõustumine` and `Ülevõtmise tähtaeg` read at their own end of the road — decided on the milestone's stable kind and never on its label, because `Jõustumine` is both a phase and a record. `Lisa tõend` becomes **`Lisa fail`** on the ordinary file action while the evidence store, the audit events and `DocumentVersion` keep the domain's own word, and `Etapiga sidumata` loses the paragraph that explained it. The round's own lesson is A4: four of five changes were *moves*, a move can sit under the visual tolerance while changing what a page means, so the placements are asserted as document facts in `e2e/test_structural_placement.py` and the visual tolerance is unchanged.
- 0100 — `Menetluse kulg` is where the file is going; `Teema käik` is what happened (succeeding 0099 after a full exploratory QA round; six decisions, **no migrations**): 0099's merged rail was trying to answer two questions with one drawing — it carried the roadmap *and* borrowed history to date it — and eight separately reported defects turned out to be that one. `Teema käik` already answers historical repetition properly, a section per occurrence in date order with its own `alates` heading, so the rail stops trying to. **`Alustatud` is gone and nothing replaces it**: it was `Matter.created_at`, a fact about this database rather than about a procedure, which drew `Saabus 1.9` in a header above `Alustatud 21.9` on the rail, placed a backdated `Koja arvamus 20.9` *before* the work began, and sat mid-sequence in dates running 5.3 → 2.4 → 21.9 → 3.6 → 17.6; the pattern's own first phase is the beginning the procedure has, and `PHASE_STARTED = 0` is retired rather than reused because `_MILESTONE_PHASE` reads the constants. **A phase node is dated by the roadmap and never by the first step filed under it** — a twice-consulted file read `Kooskõlastusring 2.4.2026` an inch above a `Teema käik` saying `alates 20.08.2026`, and borrowing history in pattern order made the dates non-monotonic because a file that goes forward and comes back is a loop while a rail is a line; consequently `Muuda` now offers a date box on **every** phase, where it used to withhold one wherever a `Märge` had dated it. **A canonical dated fact folds onto its phase** rather than being drawn beside it, so `Jõustumine · Jõustumine 1.1.2027` becomes one column carrying «põhiosa 27.9.2027» and «osad sätted 1.1.2028» as visible lines instead of a `title` no touch screen, printout or screen reader delivers. **Two phases cannot be taken off**: the current one, whose removal left the header naming a phase the rail no longer drew and reintroduced the deadline placement 0099 had just fixed, and any phase holding a canonical dated fact, whose removal stranded the fact and re-sorted it so `Jõustumine 1.1.2027` drew before `Valitsuses`; both are disabled with the reason beside the chip **and** re-asserted where the service call is built, because «the control was not rendered» is never how a rule is kept here. **Explicit roadmap dates run in the procedure's order** — `Kooskõlastusring 01.12` with `Valitsuses 25.09` drew a left-to-right time rail reading December before September — compared only against each other, with the error on the box that is out of order and the typed values kept; a file that went *back* to an earlier phase is history and belongs in `Teema käik`. **A closed file's own clock stops**: the date stays in the header because it is part of the record, but `ActiveDeadline.is_active` is false on a closed Matter — `Rohkem ei tegele` among them, since a `Disposition` is *why* a file is closed and the database refuses one on an open row — while the rail goes on drawing the phases the external procedure may still reach, which is a different sentence and stays one.
- 0101 — received feedback may name nobody (relaxing 0091 §3.3 for `RECEIVED` rows only; one additive migration, no backfill): 0091 made `organisation` nullable and gave received feedback a `source_label` for a collection of answers with no single author, then had the database refuse a row carrying neither. It was right about the problem and one answer too strict about the solution — `Allikas` names a *collection*, a single anonymous caller is not one, and a lawyer writing down what a member said on the telephone met a panel that would not save until they invented a name for who it was (OWNER-01). The rule meant to keep invented authorship off professional files was producing it, so the check becomes `organisation_id IS NOT NULL OR provenance = 'received'`: for `RECEIVED` the record may name nobody, and the absence *is* the record. **`DISCOVERED` is unchanged** — a published opinion with no author is an anonymous claim and `EXTERNAL_POSITION_NEEDS_ORGANISATION` still refuses it — and the record still cannot be empty, since `EXTERNAL_POSITION_NEEDS_SOURCE` means a position, a link or a file is always present; what became optional is *whose* it was. Naming is not weakened where it happens: the catalogue's own row, `PROTECT`, the received-only `Allikas` and both correction doors all stand. `EXTERNAL_POSITION_NEEDS_AUTHOR_OR_LABEL` is retired rather than deleted, with a note saying what happened to it, because release notes and tests cite the name. A defaulted «Nimetu» label was rejected as the same invented authorship written by the application, and inferring the member from the file's `Kaasamine` rows as the prose-parsing the product does not do.
- 0102 — a mistaken record comes off the active file (reversing 0084 §8's «no delete» rule for user-created `Teema käik` blocks; three additive migrations, no backfill): «a mistaken row is corrected, because what the file recorded and who recorded it is part of the file» has stood since `Väline seisukoht` was written and was repeated into every record type built after it. It is right about a record of something that *happened* and wrong about a row that should never have been on this Teema — and the owner met the difference in ordinary use, filing a `Märge` on the wrong file and finding that the only repair was to rewrite it into something else, which leaves a sentence nobody wrote, dated a day nobody chose, attributed to whoever was fixing it (OWNER-04). **Three meanings are kept apart**, each with its own column, service and audit event: `Tühistatud` is a plan somebody called off and its row stays saying so, `Tagasi võetud` is a letter that went out and was taken back and its row stays too, and **removed** means nothing happened, so the row leaves. It is a **column and never a `DELETE`** — `removed_at` and `removed_by` on the canonical record — so the `ChangeEvent` trail keeps what was recorded and who removed it, evidence stays in the immutable store, and `EntryRevision` is untouched, which is precisely the point: a corrected `Entry` is append-only by database trigger and is a *blocker* for whole-Matter deletion (docs/adr/0096 §4). **The filter lives in `visible_to`**, the one chokepoint that already applies authorization before any grouping, counting, ranking or projection, so the chronology, the rail, `Minu asjad`, the register, the statistics, the exports and the search corpus all needed no change — a filter written at a call site is a filter the next call site forgets. Search follows on the write, the rule `refresh_engagement` established, and the child refreshes delete for every row while inserting only the live ones so a rebuild and a per-write refresh converge. **Eight families are written down** in one table — `sissekanne`, `marge`, `kaasamine`, `seisukoht`, `ulevaade`, `tahtaeg`, `joustumine`, `toovoit` — keyed by the Estonian word the row already uses and each carrying its own `ChangeEventType`, because a shared `RECORD_REMOVED` would print «Kirje eemaldatud» eight different ways on `Kõik muudatused`; `kind_of` matches the exact class rather than `isinstance`, so no future subclass inherits the capability silently, and there is no registry, no generic `Event` table and no state machine. A **sent `Koja arvamus` is excluded by argument rather than omission** — the chronology projects `historically_sent()`, a letter that left this office, and removing it would have the record claim it never went, where the domain's own sentence is `WITHDRAWN` — as are audit and system rows, which have no canonical business record under them, and `Menetluse link`, which is Matter metadata rather than a `Teema käik` block. One **POST-only** route serves all eight, with the confirmation a native disclosure in the row rather than a second page, the record fetched through its own `visible_to`, closure enforced under the Matter's row lock rather than by whether a chip rendered, `updated_at` as the stale-tab token, and a double submit answered as the state the person asked for rather than as an error. A removed record's **files stay and their claim of belonging does not**: `DocumentLink.visible_to` drops the link, because bytes that arrived on a Matter are evidence of it and a file row reading «kuulub: …» under a record nobody can see is the page pointing at nothing. If a removed `Märge` was a phase boundary, the rows below it become `Etapiga sidumata` — truthful, where an invented replacement would be manufacturing exactly the history this record exists to protect.
- 0103 — a recorded send is correctable, and a position takes a paper that arrives later (two corrections from the 2026-09-21 QA round that touch records already on the books; one audit-choices migration, no backfill): `Teema käik` gives every user-created record a `Muuda` — feedback, others' opinions, `Kaasamine`, `Märge`, `Ülevaade / uudis` — and `Arvamus välja` carried none, on the one record where a wrong date or recipient matters most, because the send date reaches the outbound register, the process rail and every report that counts advocacy; a letter registered with the day mistyped could be repaired only by an administrator (QA-023). **This is not the editor 0061 retired**: that was a second *surface* for opinions restating the letter, its evidence mechanics and its reconciliation facts beside `Dokumendid`, and 0093 §3 held the line when it added `Arvamuse märksõnad ja seosed`. What is added is one control on the chronology row that already exists, correcting exactly the four facts that row prints — the business date and its precision, the `Liik`, the `Kokkuvõte` and the addressees — and nothing else: no file, no status, no title, no tags, no overview links, no workflow, with the send routes untouched and still landing on `Dokumendid`. A correction **may not reach** the evidence (`final_version` is not an argument, no `DocumentVersion` is superseded, and a letter whose text was wrong is a different letter), the status (un-sending, withdrawing and superseding have their own services and meanings, and a spelling fix may not become one), `sent_by` (who sent it is not who corrected the record), or the `Teadmiseks` recipients (`set_recipients` replaces the whole set, so the service reads the copied-in committees and passes them through rather than letting a date fix drop them). The act has its own name, `SUBMISSION_CORRECTED`, carrying the before and after of every value that moved and written at all only when one did — not a second `SUBMISSION_SENT`, which would make the file read as two letters, and not a `SUBMISSION_WITHDRAWN`, which says something about the letter where this says something about what we wrote down about it. Only a recorded send qualifies, a `DRAFT` being refused rather than given an invented date; concurrency is `updated_at` under the row lock; and there is **no open-Matter lock**, which is the existing contract rather than an exception — `withdraw_submission` already corrects a recorded send on a closed file. The row deliberately has no `Kustuta`, the one user-created chronology row without one (docs/adr/0102 §4). The second half extends 0084 §8 to the record it had not reached: a `Menetluse areng` has had `+ Lisa fail` since 0091 §5.4, and a `Väline seisukoht` had no such act at all — files arrived only with the capture and `Muuda` deliberately takes no bytes, so an association sending its position paper a week after somebody wrote down what it said on the telephone had nowhere to put it (QA-021). The same act with the same refusals: additive only, never a replacement or a removal, no revision token (two people attaching two different papers is not a lost update), refused on a closed Matter, all or none. The rest of QA-021's reported asymmetry is 0095 §3 and §5 working as decided — capture asks few questions, `Muuda` asks what a correction needs — and the correction form is not widened to match a panel deliberately kept short.
- 0104 — every whole-record editor carries the version it was filled from (generalising a pattern five records already had; **no migrations**): a `Märge`, a `Kaasamine`, a `Väline seisukoht`, an entry and a private note have carried a revision token for some time — the form holds the version it was filled from, the service compares it under the row lock, a stale save writes nothing — and `Muuda teemat` and the `Menetluse kulg` editor did not. The QA round found exactly what that costs: Tab A changes `Vastutaja` to Martin and saves, Tab B changes only `Lühikokkuvõte` and saves, and `Vastutaja` is Sandra again with no warning in either tab, while the audit log records the revert as an ordinary business event one second after the handover it undid. It does not need two people — one lawyer with the file open twice is enough, and that is the normal way this tool is used (QA-002, QA-004). Three properties are load-bearing: the token is compared **under the lock**, so what it is measured against is the committed version rather than whatever the caller's instance remembers; **before any value is decided**, so a refusal cannot have half-applied the record; and the conflict **carries the committed row**, so the answer shows what beat this save beside what the person typed, with neither chosen for them. The token is `updated_at.isoformat()` — `auto_now` sets it on every write, PostgreSQL stores it to the microsecond so two saves cannot share one, and it costs no migration; an edit counter would not do, because a correction reverted and re-applied returns it to a value a stale form is still holding. A record with no single row gets a token over its rows: `timeline_steps_revision_token` is a SHA-256 over the sorted `MatterTimelineStep` set, because the thing being edited is the whole rail. A refusal writes nothing, loses nothing, answers **409**, and does **not** advance the token — adopting the newer one would be the view deciding that the next submit may overwrite what the other writer saved — and it re-renders the *panel* rather than the page, through `HX-Retarget`, because swapping a panel into the page's own target replaces the document with a bare form. Evidence capture deliberately carries no token: two people attaching two different papers to one record is not a lost update, both links are wanted, and a token there would refuse the second lawyer's file to protect a sentence nobody touched. A database `version` column was rejected as a migration on eight tables to store what `updated_at` already stores, and a lock alone as the thing that serialises the two writes while still letting the second overwrite the first — what is needed is not ordering but the second writer *knowing* the first happened.
- 0105 — `Teema käik` is one list, and a `Märge` takes whatever there is (four changes to one page in the same direction; **one migration**, `matters/0037`, dropping a `CHECK` and widening a field, with no data migration, no backfill and no search-index version change): the chronology stops being grouped into the phases 0092 gave it, because the grouping had two costs and the second is the one no relabelling reaches — `Etapiga sidumata` collected the records nothing can ever place and read as a queue somebody owed work on however it was worded, and the *list stopped being chronological*, arriving newest-phase-first with the unplaced group last, so a row from May could sit below a row from the previous January; the thing the grouping was for is already on the page, because 0092 §2 itself put `Menetluse kulg` above `Teema käik` so that collapsing the history would not take «where is the procedure» with it, and that rail draws the phases in the procedure's own order, marks each recorded one `Kirjas`, dates it from the record that proves it and says `Praegu` on the current one — two sections answering one question inches apart, and the one reordering the rows answered it worse; `process_phase` is untouched and still asked, still corrected and still the only thing that marks a node, so what went is a rendering rather than an association, and dropping only the `Etapiga sidumata` heading was rejected because the rows would still sort to the foot of the page out of date order with nothing saying why. A milestone row loses a line: the headline, the day and `Muuda` / `+ Lisa fail` / `Kustuta` are one wrapping flex row in a new `.uxtl__head`, which is a `<div>` because the tree builder closes an open `p` at `Kustuta`'s `<details>` — the defect `row_remove.html` documents — and the nesting is asserted with a parser rather than a string window since the markup reads the same either way and only the tree differs; a work entry keeps its controls under its text, because they live inside the swap target a correction replaces. An `Ülevaade / uudis` row becomes its address: `Avaldatud` restated the section it was in and `Ava ülevaade või uudis` named what a link is for in place of *which page*, so a file written up three times showed three identical rows — `link_display` prints host, path and query with the scheme, any `userinfo` and the trailing slash dropped and a cut at 72 characters marked, which reverses 0085 §2's label while keeping its reason and F-2's rule, and `Paranda link` becomes `Muuda` while the cancelled row keeps `Tühistatud`. And `+ Märge` stops refusing saves whose content was complete: `Mis juhtus?` was `required=False` on the form and refused in `clean_title`, so a lawyer whose whole answer was the paper that arrived wrote a sentence restating the filename — every control is optional now, a blank title reads as the word `Märge` in the presentation layer with nothing derived from the note, the filename or the stage, and what is refused is a press carrying no sentence, no file, no stage *and* no step, as one sentence naming all four, raised in `add_procedural_development` before the row lock and repeated by the form; the next step's date stays paired because a dateless step appears in nobody's `Tähtajad` and `set_next_action` refuses `DO`/`DEADLINE` without one (0052 §3, §5), and `Etapp` stays on the panel because it is now the only way the rail gets filled. **Amended 2026-09-26** (ENG-060): a `Hetkeseis` counts only when it moves the file, a note counts as a sentence, and the rule is decided on the Matter row `lock_open_matter_for_business_write` returns rather than before the lock (`development_save_says_something`); `Muuda` may not leave a file-less `Märge` with no title and no note, and what the original save moved — the stage, the next step — is not counted as the row's content, which leaves ENG-020's removal question open
- 0106 — a next action may have no date yet (superseding 0052 §5's fourth validation row and the constraint written for it; **one migration**, `workflow/0008`, a single `RemoveConstraint` with no column change, no data migration and no backfill): a lawyer routinely knows *what* happens next before knowing *when*, and the product refused that — text and date had to arrive together, so recording «vaatan ministeeriumi vastuse üle» meant typing a day nobody had chosen, and an invented day is not a harmless placeholder but a false statement the work queue then reports on: it lands in `Tähtajad`, it goes overdue, it is counted late, and the person who invented it explains the number. The canonical storage is `DO` / `DEADLINE` / `target_date=NULL`, and reaching for `WAIT`, `MONITOR` or `EXPECTED_AROUND` to satisfy the old invariant was rejected because each is a lie about a different thing — the lawyer is not waiting, not monitoring and has not estimated; `DEADLINE` says what the date means *when there is one* and says nothing when there is not, `NULL` means «no deadline recorded yet» and is never read as today, approximate, waiting or overdue, and `date_precision` keeps its `EXACT` default beside it purely as an implementation detail since `display_date` and `date_label` both answer `""` on a `None`. `workflow_deadline_requires_a_date` is dropped and the migration is rolling-safe in the direction that matters — a `CHECK` removal cannot fail on existing data, the old image cannot write a row it would have caught because the same refusal lives in `set_next_action` and `NextActionForm`, and every old reader already guards `None` because `WAIT`/`MONITOR` have always reached the same paths — while the reverse re-adds it and *will* fail on a database that has since taken an undated `DO`, which is honest; `text` stays required and the one-open-action constraint is untouched. Three forms now agree (`NextActionForm`, `MatterProgressForm`'s `+ Märge` block, `ComposerForm._clean_next_action`), a date with no sentence stays refused **on the sentence**, a malformed period stays refused on its own control, and adding or clearing a date later is the ordinary supersede through `set_next_action_for_new_work` with the replacement chain and audit event unchanged. The surfaces needed almost nothing: `my_work.undated_items` has rendered a `Kuupäevata` block since the page was built and simply had only WAIT/MONITOR rows to put in it, `PortfolioRow.has_action` already counts an undated action so *järgmine tegevus puudub* correctly excludes it, and every dated surface — `overdue_date_q`, `due_for_review`, `?tegevus=hilinenud`, `?tegevus=ulevaatus`, the upcoming windows, `OVERDUE_DO_DEADLINE`, `REVIEW_DUE`, `my_work_timeline`'s four dated bands — excludes a `NULL` in SQL rather than by a Python guard that could drift, which was audited consumer by consumer rather than assumed; what was added is the wording, `Kuupäev määramata` in `PRAEGUNE TEGEVUS` and on the portfolio row, muted and never the overdue colour because a step with no deadline recorded cannot be late, in the construction `Etapp määramata` and `Vastutaja määramata` already use and deliberately not «Tähtaeg määramata», since *tähtaeg* is reserved for the two obligations owed outside (0054 §Amendment). Historical WAIT/MONITOR/REVIEW_ON/EXPECTED_AROUND semantics, the register parser, the `Uus teema` prepare-by flow and `INDEX_VERSION` are all untouched. Also in this round: `opinion_manage.html` prints the address through the same `link_display` 0105 §3 gave the chronology row, finishing the one inconsistency that round left — two presentations of one link on one file — with the stored `url` untouched and no second URL formatter written
- 0107 — a column swap keeps what was typed, and a failed request is told (amending ADR 0075 §3's premise, not its decision; **no migrations**, no search-index change): the whole-column swap stays for ADR 0074 §10's reason, but every place a lawyer types is a `data-draft-host`, and a host holding unsaved input — other than the one that sent the request — is carried across the swap with htmx's `hx-preserve` into its own place, or shown as read-only `Salvestamata sisu` when its record moved on; nothing is stored in the browser. Every failure that is not an inline refusal (5xx, network, timeout, 403, 404, an expired session, a stale CSRF token) is told in the form that sent it, in Estonian, and never retried; an htmx request that must sign in gets a 401 instead of a followed redirect, a CSRF refusal a marked 403, and 404/500/CSRF pages are Estonian (ENG-012, ENG-034).
- 0108 — `Sarnased teemad` asks by POST, with only the fields that decide it (amending ADR 0087 §4 and reversing its rejected «post the draft form» alternative; **no migrations**, no search-index change): the region asked with a GET of `closest form`, so the private `Märkmed`, the CSRF token and every other field went into the address and every log that records one, and a long summary overran the server's request line; three of its five triggers named ids no element carries, so a Valdkond, Õigusakt or Saatja pick asked nothing. The route now takes POST only (GET is a 405) and still writes nothing, the region names its parameters with `hx-params`, and the chips are heard at the form, filtered by their real `name`, with a short delay folding a burst into one request (ENG-026, ENG-090).
- 0109 — Matter writes lock the Matter before the search index, and inline whole-value editors carry the version of their own value (amending ADR 0040 §3 and extending ADR 0104 §1; **no migrations**, no search-index change): `close_matter` and `add_evidence_version` take `FOR NO KEY UPDATE`, which still serialises closures and uploads but no longer blocks the `FOR KEY SHARE` a running rebuild needs at COMMIT, closing the rebuild-gate deadlock (ENG-027); `set_policy_areas`, `set_organisations` and `set_tags` lock the Matter before `.set()` fires the search refresh, so overlapping saves of one Teema queue on the Matter instead of deadlocking or colliding on the projection's unique index (ENG-029); the inline Valdkonnad, Saatja and Lühikokkuvõte editors carry a digest of their own value — not the Matter's `updated_at`, because each fragment re-renders alone — checked under that same Matter lock, a missing token is a conflict, and the three structured-fact editors carry their row's `updated_at`; a stale save answers 409 and writes nothing (ENG-028).
- 0110 — workflow transitions and owner assignment decide on the locked row (amending ADR 0075 §4; **no migrations**): `complete_next_action`, `cancel_next_action` and `acknowledge_review` lock the Matter and then the action at `FOR NO KEY UPDATE` and decide on the re-read row, so a racing replacement, closure or second click is refused instead of leaving a COMPLETED action with `replaced_by`, a completion after MATTER_CLOSED, two completion events or a review on a superseded step (ENG-072); `assign_matter` takes the Matter lock before touching notices — the order `delete_matter` uses — refuses a deleted Matter, reads the audit's `from` from the locked row, and still lets a closed Matter change hands (ENG-075); no `replaced_by`/SUPERSEDED CHECK is added.
- 0111 — a write that points one Matter at another locks both (amending ADR 0096 §4.6; **no migrations**): `close_matter` with a successor, `link_related_matters`, `dismiss_related_suggestion` with a candidate Matter and `add_background_submission` lock both Matters at `FOR NO KEY UPDATE` in ascending-id order through `lock_matters_in_order` and refuse a deleted counterpart, so `delete_matter` — which plans under the deleted Matter's own lock — can no longer commit alongside a new pointer at it and leave a live Matter naming a tombstone or a relation under one (ENG-073); the deletion itself is unchanged.
- 0112 — an `Uus teema` form is saved at most once (extending ADR 0064's intake session; **one migration**, `matters/0039`, an additive nullable `matter` foreign key on the intake session, no data migration): every rendered form gets its own intake session, which is its one-time token; `Loo teema` consumes it with one conditional UPDATE first thing in the creating transaction, so a repeated or simultaneous POST of the same form waits, finds it consumed, writes nothing — no second Teema, no second reference number, no staged file promoted twice — and is redirected to the Teema the first submission created (ENG-074); a new form with the same words is a second Teema, idempotency being per form and never by content; the other additive forms are not covered and are left for a separate decision.
- 0113 — every document is findable by its name, and the projection holds text (amending ADR 0072's search consequence; **one migration**, `search/0010`, metadata only — the version default and the source-kind choices, `(no-op)` in SQL): each Document gets a `DOCUMENT` search row with its title and every version's filename, authorized through its own override and the Matter, so an upload nobody extracted is findable by name (ENG-030); `plain_text` decodes entities once and keeps block boundaries as spaces, so snippets stop showing `&amp;` and adjacent paragraphs stop fusing into one word, with template autoescaping still the only escaping layer (ENG-082); every authored body is bounded to 200,000 characters in the projection only, so a very long summary or note saves instead of failing PostgreSQL's 1 MiB tsvector limit (ENG-084); `INDEX_VERSION` moves once, `TEEMA.1` → `DOKUMENT.1`, and a production `rebuild_search_index` is required after deploy.
- 0114 — search checks check what they claim, and the manual repair repays (amending ADR 0041; **no migrations**): `check_search_integrity` derives crossings, text drift and repair hints for every source kind from one registry held complete against `SearchSourceKind`, samples each kind deterministically across the corpus by default and recomputes every row with `--full`, and says which mode it ran (ENG-080); `rebuild_search_index` claims the debt that exists when it starts and deletes exactly that after its atomic rebuild commits, so freshness goes green with the index and debt written meanwhile survives (ENG-085); the release manifest states whether `INDEX_VERSION` moved, and the final `deployment_readiness` fails on search rows built under an older version while `--search-phase=pre-rebuild` reports the expected state before step 11's rebuild (ENG-142).
- 0115 — CI proves what it says it proves (**no migrations**): in CI a skip is either one `ci_skip_policy.py` names, by test and stated reason, or a failure, and a missing E2E variable fails the browser fixture instead of skipping it (ENG-051); the `postgres-safety` step-back starts from a seeded database, and every data migration the range cannot reach has a named populated proof held complete by a guard (ENG-052); the quality job runs `uv lock --check`, so `pyproject.toml` and `uv.lock` cannot disagree (ENG-053); the shard-completeness proof parses each suite's selecting arguments from the workflow's own `run:` lines and compares their union with the unfiltered universe (ENG-110).
- 0116 — search answers from its indexes, and every result is reachable (resolving the open decision on search at corpus scale, amending ADR 0014's partial trigram indexes; **one migration**, `search/0011`, indexes only, built `CONCURRENTLY`): every matching tier is a predicate an index on the projection serves — `ILIKE` for substrings, the `%>` operator for fuzzy titles, the exact title narrowed through the projection's own title — so a selective search is a bitmap over its indexes instead of a scan of the whole projection; the ranking statement carries ids and sort keys and the page's rows are fetched afterwards; the count is a plain `COUNT(*)`; collaborator visibility is an uncorrelated subquery, so nothing needs a `DISTINCT` (ENG-010); `/otsing/` is paginated with the register's component and `leht` parameter, so every authorized result is reachable (ENG-048); a frozen copy of the previous query holds the answers row for row, and `INDEX_VERSION` is unchanged.
- 0117 — search finds Estonian word forms, and says what matched (**one migration**, `search/0012`: a `people_text` column with a database default, a diacritic-folded `search_folded` vector, and their two indexes built `CONCURRENTLY`): a diacritic-free tier and a word-beginning tier — the query word's Estonian stem as a prefix, and the typed word as a prefix of the folded text — both served by GIN, so «Tarbijakaitseseadus» finds «…seaduse…» and «tahtaja» finds «tähtaja», with stem alternation (`tähtaeg`/`tähtaja`) left as a documented residual; `Kaasamine`, `Märge` and received opinions join the title and alias tiers (ENG-031); a Matter carrying a merged tag is found through its successor, and a merge refreshes only the Matters it affects (ENG-081); results name the record that matched, a received opinion's summary is quoted, and an author hit says «Autor» (ENG-083); phone-width result titles take the row and two lines (ENG-100); `INDEX_VERSION` moves once, `DOKUMENT.1` → `SONAVORM.1`, and a production rebuild is required.
- 0118 — a full rebuild builds a new generation beside the one in use (amending ADR 0013's one-transaction rebuild and ADR 0041's cost to a save; **one migration**, `search/0013`: a `SearchGeneration` table, a `generation` column with a database default, the one-row-per-source uniqueness widened by generation — new index built and old dropped `CONCURRENTLY` — and a generation index): readers read the active generation only, targeted refreshes write every live generation, a rebuild fills the next one in short batches under the refresh gate and swaps it in in one short transaction, so a save waits for at most one batch rather than the whole rebuild (ENG-011); one rebuild at a time; a writer waiting at the gate can no longer be chosen as a deadlock victim; `INDEX_VERSION` unchanged.
