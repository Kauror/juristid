# Create the GitHub repository

Create a **private** repository named:

`Kauror/juristid`

Recommended GitHub UI settings:
- Visibility: Private
- Do not initialize with a framework/template
- Default branch: `main`

Then add the contents of this bootstrap directory as the initial commit.

The authoritative specification should end up at:

`docs/master-specification.md`

After the initial commit, give the coding agent `prompts/stage-0-coding-agent.md` and authorize **Stage 0 only**.
